// src/playground/forcedPlayerIdentity.ts
var FORCED_PLAYER_PARAM = "rundotPlayer";
var PLAYER_ID_REGEX = /^[a-zA-Z0-9_]{1,32}$/;
function parseForcedPlayerId(search) {
  if (!search) return null;
  const raw = new URLSearchParams(search).get(FORCED_PLAYER_PARAM);
  if (raw === null) return null;
  const trimmed = raw.trim();
  if (trimmed.length === 0) return null;
  if (!PLAYER_ID_REGEX.test(trimmed)) {
    console.warn(
      `[RUN:playground] Ignoring invalid ?${FORCED_PLAYER_PARAM}= value "${raw}" \u2014 must match ${PLAYER_ID_REGEX}. Falling back to automatic player assignment (first browser/tab = base identity; tabs in the same window get distinct players automatically). To pin a distinct player in a separate browser or context, use a valid value, e.g. ?${FORCED_PLAYER_PARAM}=player2.`
    );
    return null;
  }
  return trimmed;
}
function stripForcedPlayerParamFromUrl() {
  if (typeof window === "undefined") return;
  const url = new URL(window.location.href);
  if (!url.searchParams.has(FORCED_PLAYER_PARAM)) return;
  url.searchParams.delete(FORCED_PLAYER_PARAM);
  window.history.replaceState(null, "", url.toString());
}

export { FORCED_PLAYER_PARAM, parseForcedPlayerId, stripForcedPlayerParamFromUrl };
//# sourceMappingURL=chunk-NCDU6CSM.js.map
//# sourceMappingURL=chunk-NCDU6CSM.js.map