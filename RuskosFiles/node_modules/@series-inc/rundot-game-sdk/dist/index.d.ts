import { s as RpcRequest, t as RpcResponse, u as RpcNotification, A as AnalyticsApi, v as RpcClient, R as RundotGameAPI, H as Host, w as StorageApi, N as NavigationApi, Q as QuitOptions, x as NavigationStackInfo, y as PushAppOptions, z as NavigateToGameOptions, C as NotificationsApi, D as ScheduleLocalNotification, E as SubmitMessageInput, F as SubmitMessageResult, G as ScheduleNotificationOptions, I as RCSAvailabilityStatus, J as ScheduleRCSInput, K as ScheduleRCSResult, L as RequestRCSOptInInput, M as RequestRCSOptInResult, O as PopupsApi, T as ShowToastOptions, U as LikeDialogResult, V as CommentsPanelResult, W as LikeStateResult, X as ShowInterstitialAdOptions, Y as ShowRewardedAdOptions, Z as ProfileApi, P as Profile, _ as DeviceApi, $ as DeviceInfo, a0 as EnvironmentApi, a1 as EnvironmentInfo, a2 as SystemApi, a3 as SafeArea, a4 as AddToHomeScreenResult, a5 as CdnApi, a6 as FetchFromCdnOptions, a7 as AssetUrlResult, a8 as TimeApi, a9 as ServerTimeData, aa as GetFutureTimeOptions, ab as AiApi, ac as AiChatCompletionRequest, ad as AiChatCompletionData, ae as PromptCompletionRequest, af as AiChatCompletionStreamOptions, ag as AiChatCompletionStreamChunk, ah as HapticsApi, ai as HapticFeedbackStyle, aj as GamepadSnapshot, ak as GamepadConnectionEvent, al as Subscription, am as GamepadApi, an as GamepadLatencyStats, ao as FeaturesApi, ap as Experiment, aq as ExperimentAssignment, ar as LiveOpsApi, as as LiveOpsConfigResult, at as LiveOpsRawConfig, au as LiveOpsSection, av as LifecycleApi, aw as SleepCallback, ax as AwakeCallback, ay as PauseCallback, az as ResumeCallback, aA as QuitCallback, aB as BackButtonCallback, aC as IdentityChangedCallback, aD as IdentityChangedEvent, aE as SimulationApi, aF as SimulationSlotValidationResult, p as SimulationSubscribeOptions, aG as SimulationBatchOperation, aH as SimulationBatchOperationsResult, aI as SimulationAvailableItem, aJ as SimulationPowerPreview, aK as SimulationSlotMutationResult, aL as SimulationSlotContainer, aM as SimulationAssignment, aN as SimulationState, i as RundotGameSimulationConfig, aO as ExecuteRecipeOptions, aP as ExecuteRecipeResponse, aQ as CollectRecipeResult, aR as ResetStateOptions, aS as ResetStateResult, aT as GetActiveRunsOptions, n as SimulationRunSummary, aU as ExecuteScopedRecipeOptions, aV as ExecuteScopedRecipeResult, aW as GetAvailableRecipesOptions, aX as GetAvailableRecipesResult, aY as Recipe, b as RecipeRequirementResult, aZ as GetBatchRecipeRequirements, a_ as TriggerRecipeChainOptions, a$ as RoomDataUpdate, b0 as RoomMessageEvent, b1 as ProposedMoveEvent, b2 as RundotGameTransport, b3 as RoomsApi, b4 as CreateRoomOptions, h as RundotGameRoom, b5 as JoinOrCreateRoomOptions, b6 as JoinOrCreateResult, b7 as ListRoomsOptions, b8 as UpdateRoomDataOptions, b9 as RoomMessageRequest, ba as StartRoomGameOptions, bb as ProposeMoveRequest, bc as ProposeMoveResult, bd as ValidateMoveVerdict, be as ValidateMoveResult, bf as RoomSubscriptionOptions, bg as LoggingApi, bh as IapApi, bi as SpendCurrencyOptions, bj as SpendCurrencyResult, bk as SubscriptionTier, bl as RunSubscriptionsResponse, bm as SubscriptionInterval, bn as PurchaseSubscriptionResponse, bo as OpenStoreResult, bp as GetCurrencyIconOptions, bq as LoadEmbeddedAssetsResponse, br as DirectPurchaseSku, bs as SharedAssetsApi, bt as CreditsApi, bu as CreditsBillingContext, bv as CreditBalance, bw as CreditSubscription, bx as CreditPlansCatalog, by as GenerationCostEstimateRequest, bz as GenerationCostEstimate, bA as OpenPaywallOptions, bB as CreditsPurchaseResult, bC as Unsubscribe, bD as PreloaderApi, bE as SocialApi, bF as ShareMetadata, bG as ShareLinkResult, bH as SocialQRCodeOptions, bI as QRCodeResult, bJ as ShareClickData, bK as ShareFileOptions, bL as ShareFileResult, bM as CanShareFileResult, bN as ComposeSocialPostOptions, bO as ComposeSocialPostResult, bP as FollowMeResult, bQ as FollowMePlatform, bR as EntitlementApi, bS as Entitlement, bT as LedgerEntry, bU as StatsApi, bV as GrantInfo, bW as CollectiblesApi, bX as CollectibleCard, bY as VipClaimResult, bZ as ShopApi, b_ as StorefrontResponse, b$ as StorefrontItem, c0 as ShopPurchaseResult, c1 as ShopPurchaseResponse, c2 as ShopOrderHistoryResponse, c3 as PromptLoginResult, c4 as AccessTier, c5 as AccessGateApi, c6 as TextGenApi, c7 as UgcApi, c8 as MultiplayerApi, c9 as VideoApi, ca as ActivityApi, cb as ActivityIntent, cc as StartActivityResult, cd as UpdateActivityInput, ce as EndActivityInput, cf as ActivityActionEvent, cg as Avatar3dApi, ch as AssetManifest, ci as Avatar3dConfig, cj as ShowEditorOptions, ck as Avatar3dEdits, cl as AdsApi, cm as RawEnvironmentInfo, cn as SharedStorageHostApi, co as FilesApi, cp as ClipsApi, cq as ChoicesApi, cr as AttributionApi, cs as InitializationContext, ct as InitializationOptions, cu as CaptureConsent, cv as StartClipRecordingOptions, cw as ClipBlob, cx as ClipsSupport, cy as ClipPersistOptions, cz as ClipResult, cA as UgcEntry, cB as CaptureConsentStatus } from './AdsApi-Ds6PEEd8.js';
export { cC as AccessDeniedError, cD as ActivityState, cE as AiContentBlock, cF as AiImageContent, cG as AiImageUrlContent, cH as AiMessage, cI as AiResponseFormat, cJ as AiTextContent, cK as AiToolResultContent, cL as AiToolUseContent, cM as Asset, cN as AssignedVariant, cO as BUCKET_COUNT, cP as CLIP_CONTENT_TYPE, cQ as COMPOSE_POST_MAX_TEXT_LENGTH, cR as COMPOSE_POST_MAX_TITLE_LENGTH, cS as COMPOSE_POST_SUBREDDIT_PATTERN, cT as CREDITS_EXHAUSTED_CODE, cU as Category, cV as ChatMessageData, cW as ClipAudioOptions, cX as ClipCameraOptions, cY as ClipPipLayout, cZ as ClipPipPosition, c_ as ComposeSocialPostMedia, c$ as ConnectionState, d0 as CreditFreeDailyInfo, d1 as CreditPlan, d2 as CreditTopUpPack, d3 as CreditsBilledTo, d4 as CreditsExhaustedError, d5 as CreditsExhaustedErrorInfo, d6 as CreditsPurchaseOutcome, d7 as CurrencyIconSize, d8 as CurrencyIconVariant, d9 as DirectPurchaseGrantKind, da as DirectPurchaseStorePlatform, db as DirectPurchaseStorePrice, dc as FOLLOW_ME_PLATFORMS, dd as GAMEPAD_BUTTON_NAMES, de as GamepadAxes, df as GamepadButtonName, dg as GamepadButtonState, dh as GamepadSource, di as GenerationCostEstimateKind, dj as GetSubscriptionsForTierRequest, dk as HudInsets, dl as IdentityChangeReason, dm as InboundForKeyEntry, dn as InboundMethodIds, dp as InboundStorageApi, dq as IsPlayerSubscribedRequest, dr as JoinOrCreateRoomEnvelopeResponse, ds as JoinRoomMatchCriteria, dt as ListUserRoomsOptions, du as LiveOpsExperiment, dv as LiveOpsOverride, dw as LiveOpsVariant, dx as LoadEmbeddedAssetBeginRequest, dy as LoadEmbeddedAssetBeginResponse, dz as LoadEmbeddedAssetChunkRequest, dA as LoadEmbeddedAssetChunkResponse, dB as LoadEmbeddedAssetsRequest, dC as MatchmakeOptions, dD as MediaPlaybackAction, dE as MediaPlaybackActivityIntent, dF as MediaPlaybackState, dG as MediaPlaybackStatus, dH as MockAvatarApi, dI as NotificationTriggerInput, dJ as OnNotificationCallback, dK as OnRequestCallback, dL as OnResponseCallback, dM as PlatformCapabilities, dN as ProposedMovePayload, dO as Protocol, dP as PurchaseSubscriptionRequest, dQ as RCSAvailabilityReason, dR as RCSOptInStatus, dS as ROOM_GAME_PHASES, dT as RealtimeRoomSummary, dU as RecipeInfo, dV as ResolvedLiveOps, dW as RoomEnvelopeResponse, dX as RoomEvents, dY as RoomGamePhase, dZ as RoomMessageEventType, d_ as RoomMessagePayload, d$ as RoomsEnvelopeResponse, e0 as RpcInboundStorageApi, e1 as RpcSharedAssetsApi, e2 as RpcTransport, e3 as RunSubscription, e4 as RundotGameRoomCustomMetadata, e5 as RundotGameRoomPayload, e6 as RundotGameRoomRules, e7 as RundotGameRoomRulesGameState, e8 as SHARE_FILE_ALLOWED_MIME_TYPES, e9 as SHARE_FILE_MAX_SIZE_BYTES, ea as SOCIAL_COMPOSE_PLATFORMS, eb as ScheduleRCSStatus, ec as ServerPlayer, ed as ServerRoom, ee as ShareFileMimeType, ef as ShopOrder, eg as ShopPurchaseCancelledResponse, eh as SimulationBatchOperationAssign, ei as SimulationBatchOperationRemove, ej as SimulationBatchOperationResult, ek as SimulationPersonalState, el as SimulationRoomActiveRecipe, em as SimulationRoomState, en as SocialComposePlatform, eo as StorefrontCollection, ep as StorefrontCollectionItem, eq as SubPath, er as SubmitMessageChannel, es as SubmitMessageChannelResult, et as SubmitMessageResultChannel, eu as TIER_SUPPORTED_INTERVALS, cE as TextGenContentBlock, cF as TextGenImageContent, cG as TextGenImageUrlContent, cH as TextGenMessage, cI as TextGenResponseFormat, cJ as TextGenTextContent, ev as TextGenTool, ew as TextGenToolChoice, cK as TextGenToolResultContent, ex as TextGenToolUse, cL as TextGenToolUseContent, ey as TimeIntervalTriggerInput, ev as Tool, ew as ToolChoice, ex as ToolUse, ez as asCreditsExhaustedError, eA as assertTierSupportsInterval, eB as assignVariant, eC as assignmentBucket, eD as createHost, eE as fnv1a32, eF as isActivityActionEvent, eG as isCreditsExhaustedError, eH as isMediaPlaybackAction, eI as mapCreditsExhaustion, eJ as resolveCollectionItemPrice, eK as resolveLiveOpsSection, eL as toPromptWireRequest } from './AdsApi-Ds6PEEd8.js';
import { B as BackendHttpDeps } from './callRemoteFunction-DeGLqhjj.js';
export { R as RemoteFunctionInvoker } from './callRemoteFunction-DeGLqhjj.js';
import { M as LeaderboardApi, N as ScoreToken, O as SubmitScoreParams, P as SubmitScoreResult, Q as GetPagedScoresOptions, W as PagedScoresResponse, X as PlayerRankOptions, Y as PlayerRankResult, Z as GetPodiumScoresOptions, _ as PodiumScoresResponse, I as ImageGenApi, A as AudioGenApi, q as SpriteGenApi, T as ThreeDGenApi, a as ImageGenParams, b as ImageGenResult, D as DepthEstimationParams, c as DepthEstimationResult, R as RemoveBackgroundParams, d as RemoveBackgroundResult, U as UpscaleImageParams, e as UpscaleImageResult, f as ImageGenJobEvent, r as SpriteGenParams, s as SpriteGenResult, t as AnimateSpriteParams, u as AnimateSpriteResult, C as CharacterAnimateParams, v as CharacterWorkflow, w as SpriteGenCosts, x as SpriteGenJobEvent, V as VideoGenApi } from './LeaderboardApi-BjkDKovD.js';
export { a0 as BgRemovalModel, a1 as BiRefNetVariant, a2 as CharacterAnimateResult, a3 as CharacterAnimationOutput, a4 as ImageGenModel, a5 as LeaderboardAntiCheatConfig, a6 as LeaderboardConfig, a7 as LeaderboardDisplaySettings, a8 as LeaderboardEntry, a9 as LeaderboardModeConfig, aa as LeaderboardPeriodConfig, ab as LeaderboardPeriodType, ac as PodiumScoresContext, $ as ProviderErrorDetail, ad as RateLimitedError, ae as RemoveBackgroundOptions, af as RundotApiError, ag as SpriteGenGeneratedAsset, ah as SpriteGenModel, ai as SubmitScoreFailureReason, aj as UpscaleImageModel } from './LeaderboardApi-BjkDKovD.js';
import { A as AppApi, a as AdminUgcApi, b as AdminImageGenApi, c as AdminVideoGenApi, d as AdminSpriteGenApi, e as AdminAudioGenApi, f as AdminThreeDGenApi, g as AppRole, R as ResolveLaunchIntentOptions, L as LaunchIntent, h as ReleaseNote, i as AdminUgcBrowseParams, j as AdminUgcBrowseResponse, k as AdminUgcListReportsParams, l as AdminUgcListReportsResponse, m as AdminUgcResolveAction, n as AdminGenBrowseParams, o as AdminGenBrowseResponse, I as ImageGenEntry, p as AdminGenListReportsParams, q as AdminGenListReportsResponse, r as ImageGenReport, s as AdminGenResolveAction, S as SpriteGenEntry, t as SpriteGenReport, T as ThreeDGenEntry, u as ThreeDGenReport } from './AppApi-4oIwAmvI.js';
export { v as AdminGenApi, w as AdminImageGenBrowseParams, x as AdminImageGenBrowseResponse, y as AdminImageGenListReportsParams, z as AdminImageGenListReportsResponse, B as AdminImageGenResolveAction, C as AdminSpriteGenBrowseParams, D as AdminSpriteGenBrowseResponse, E as AdminSpriteGenListReportsParams, F as AdminSpriteGenListReportsResponse, G as AdminSpriteGenResolveAction, H as AdminThreeDGenBrowseParams, J as AdminThreeDGenBrowseResponse, K as AdminThreeDGenListReportsParams, M as AdminThreeDGenListReportsResponse, N as AdminThreeDGenResolveAction, O as AdminUgcEntry, P as AdminVideoGenBrowseParams, Q as AdminVideoGenBrowseResponse, U as AdminVideoGenListReportsParams, V as AdminVideoGenListReportsResponse, W as AdminVideoGenResolveAction, X as AudioGenEntry, Y as AudioGenReport, Z as LaunchIntentKind, _ as ReleaseType, $ as UgcReport, a0 as VideoGenEntry, a1 as VideoGenReport } from './AppApi-4oIwAmvI.js';
export { WEBVIEW_LIBRARY_SHIM_SOURCE, getWebviewLibraryShimSource } from './webview/index.js';

type RpcMessage = RpcRequest | RpcResponse | RpcNotification;

/**
 * True when an RPC rejection is the host's "no handler registered for this
 * message id" reply — the signal that the host predates a given (additive)
 * protocol and the caller should take its legacy path. The host replies with
 * `Unsupported message type: <id>` whenever a requestId-bearing message has
 * no registered handler (client H5MessageBus).
 *
 * Detection matches HostCdnApi.isUnsupportedMessageError: the rejection may
 * surface as an Error (RundotApiError) or as a raw `{ error }` payload.
 */
declare function isUnsupportedMessageError(error: unknown): boolean;

declare class MockAnalyticsApi implements AnalyticsApi {
    recordCustomEvent(eventName: string, payload: Record<string, any>): Promise<void>;
    trackFunnelStep(stepNumber: number, stepName: string, funnelName?: string, funnelOrder?: number): Promise<void>;
}

declare class RpcAnalyticsApi implements AnalyticsApi {
    private readonly rpcClient;
    constructor(rpcClient: RpcClient);
    recordCustomEvent(eventName: string, payload: Record<string, any>): Promise<void>;
    trackFunnelStep(stepNumber: number, stepName: string, funnelName?: string, funnelOrder?: number): Promise<void>;
}

declare function initializeAnalytics(rundotGameApiInstance: RundotGameAPI, host: Host): void;

type StorageType = 'deviceCache' | 'appStorage' | 'ownerStorage';
declare function createMockStorageApi(storageType: StorageType, appUrl?: string): StorageApi;
declare class MockStorageApi implements StorageApi {
    private readonly prefix;
    private readonly syncDelay;
    private readonly orderStorageKey;
    constructor(prefix: string, syncDelay: number);
    clear(): Promise<void>;
    getAllItems(): Promise<string[]>;
    getAllData(): Promise<Record<string, string>>;
    getItem(key: string): Promise<string | null>;
    key(index: number): Promise<string | null>;
    length(): Promise<number>;
    removeItem(key: string): Promise<void>;
    setItem(key: string, item: string): Promise<void>;
    setMultipleItems(entries: {
        key: string;
        value: string;
    }[]): Promise<void>;
    removeMultipleItems(keys: string[]): Promise<void>;
    private buildKey;
    private extractKey;
    private keys;
    private simulateSyncDelay;
    private readOrder;
    private normalizeOrder;
    private rebuildOrderFromStorage;
    private upsertOrder;
    private bulkUpsertOrder;
    private removeFromOrder;
    private bulkRemoveFromOrder;
    private writeOrder;
    private clearOrder;
    private dedupeKeys;
}

interface MethodIds {
    clear: string;
    getItem: string;
    getKey: string;
    length: string;
    removeItem: string;
    setItem: string;
    getAllItems?: string;
    getAllData?: string;
    removeMultipleItems?: string;
    setMultipleItems?: string;
}
declare class RpcStorageApi implements StorageApi {
    private readonly rpcClient;
    private readonly methodIds;
    /**
     * Extra body fields merged into every RPC call. Used by shared-scope
     * bindings to carry (targetAppId, namespace) alongside the op-specific
     * arguments. `undefined` for app/owner bindings.
     */
    private readonly extraBodyFields?;
    constructor(rpcClient: RpcClient, methodIds: MethodIds, extraBodyFields?: Record<string, string>);
    private body;
    clear(): Promise<void>;
    getItem(key: string): Promise<string | null>;
    setItem(key: string, value: string): Promise<void>;
    key(index: number): Promise<string | null>;
    length(): Promise<number>;
    removeItem(key: string): Promise<void>;
    removeMultipleItems(keys: string[]): Promise<void>;
    getAllItems(): Promise<string[]>;
    getAllData(): Promise<Record<string, string>>;
    setMultipleItems(items: {
        key: string;
        value: string;
    }[]): Promise<void>;
}

/**
 * Method IDs for the legacy global-storage fallback. Mirrors `MethodIds` minus
 * `getAllData` — the legacy host only exposes `getAllItems`, which returns a
 * `Record<string, string>` (the data) rather than just keys.
 */
interface LegacyGlobalMethodIds {
    clear: string;
    getItem: string;
    getKey: string;
    length: string;
    removeItem: string;
    setItem: string;
    getAllItems: string;
    setMultipleItems: string;
    removeMultipleItems: string;
}
/**
 * SharedStorage handle that fires both the M3 `SHARED_STORAGE_*` family and
 * the legacy `GLOBAL_STORAGE_*` family on every operation. Whichever family
 * the host registers handlers for wins; the other returns synchronously with
 * an "Unsupported message type" error which we suppress.
 *
 * Scope `(targetAppId, namespace, sourceAppId)` is dropped on the legacy
 * wire — the legacy host only knows a flat per-user namespace. Two games
 * writing the same raw key on a legacy host will collide; this is a known
 * trade-off for the transition window. See the migration plan for details.
 */
declare class DualEmitStorageApi implements StorageApi {
    private readonly rpcClient;
    private readonly sharedIds;
    private readonly sharedScope;
    private readonly globalIds;
    constructor(rpcClient: RpcClient, sharedIds: MethodIds, sharedScope: {
        targetAppId?: string;
        namespace: string;
    }, globalIds: LegacyGlobalMethodIds);
    private sharedBody;
    clear(): Promise<void>;
    getItem(key: string): Promise<string | null>;
    setItem(key: string, value: string): Promise<void>;
    key(index: number): Promise<string | null>;
    length(): Promise<number>;
    removeItem(key: string): Promise<void>;
    removeMultipleItems(keys: string[]): Promise<void>;
    getAllItems(): Promise<string[]>;
    getAllData(): Promise<Record<string, string>>;
    setMultipleItems(items: {
        key: string;
        value: string;
    }[]): Promise<void>;
}

declare function initializeStorage(rundotGameApiInstance: RundotGameAPI, host: Host): void;

declare class MockNavigationApi implements NavigationApi {
    private readonly rundotGameApi;
    constructor(rundotGameApi: RundotGameAPI);
    requestPopOrQuit(options?: QuitOptions): Promise<boolean>;
    getStackInfo(): NavigationStackInfo;
    popApp(): Promise<void>;
    pushApp(appId: string, options?: PushAppOptions): Promise<void>;
    navigateToGame(targetGameId: string, options?: NavigateToGameOptions): Promise<void>;
}

declare class RpcNavigationApi implements NavigationApi {
    private readonly rundotGameApi;
    private readonly rpcClient;
    constructor(rpcClient: RpcClient, rundotGameApi: RundotGameAPI);
    requestPopOrQuit(options?: QuitOptions): Promise<boolean>;
    getStackInfo(): NavigationStackInfo;
    popApp(): Promise<void>;
    pushApp(appId: string, options?: PushAppOptions): Promise<void>;
    navigateToGame(targetGameId: string, options?: NavigateToGameOptions): Promise<void>;
}

declare function initializeStackNavigation(rundotGameApi: RundotGameAPI, host: Host): void;

declare class MockNotificationsApi implements NotificationsApi {
    private readonly rundotGameApi;
    constructor(rundotGameApi: RundotGameAPI);
    cancelNotification(notificationId: string): Promise<boolean>;
    getAllScheduledLocalNotifications(): Promise<ScheduleLocalNotification[]>;
    isLocalNotificationsEnabled(): Promise<boolean>;
    submitMessageAsync(input: SubmitMessageInput): Promise<SubmitMessageResult>;
    scheduleAsync(title: string, body: string, seconds: number, notificationId?: string, options?: ScheduleNotificationOptions): Promise<string | null>;
    setLocalNotificationsEnabled(enabled: boolean): Promise<boolean>;
    getRCSAvailableAsync(): Promise<RCSAvailabilityStatus>;
    scheduleRCSAsync(input: ScheduleRCSInput): Promise<ScheduleRCSResult>;
    requestRCSOptInAsync(input?: RequestRCSOptInInput): Promise<RequestRCSOptInResult>;
}

type DeprecationNoteDeps = {
    today: () => string;
    getLastNotedDay: (key: string) => string | null;
    setLastNotedDay: (key: string, day: string) => void;
    warn: (message: string) => void;
    recordEvent?: (eventName: string) => void;
};

type RpcNotificationsApiOptions = Partial<DeprecationNoteDeps> & {
    recordDeprecationEvent?: (eventName: string) => void;
};
declare class RpcNotificationsApi implements NotificationsApi {
    private readonly rpcClient;
    private readonly options;
    constructor(rpcClient: RpcClient, options?: RpcNotificationsApiOptions);
    submitMessageAsync(input: SubmitMessageInput): Promise<SubmitMessageResult>;
    scheduleAsync(title: string, body: string, seconds: number, notificationId?: string, options?: ScheduleNotificationOptions): Promise<string | null>;
    cancelNotification(id: string): Promise<boolean>;
    getAllScheduledLocalNotifications(): Promise<ScheduleLocalNotification[]>;
    isLocalNotificationsEnabled(): Promise<boolean>;
    setLocalNotificationsEnabled(enabled: boolean): Promise<boolean>;
    getRCSAvailableAsync(): Promise<RCSAvailabilityStatus>;
    scheduleRCSAsync(input: ScheduleRCSInput): Promise<ScheduleRCSResult>;
    requestRCSOptInAsync(input?: RequestRCSOptInInput): Promise<RequestRCSOptInResult>;
    private deprecationDeps;
}
type NotificationContent = {
    title: string | null;
    subtitle: string | null;
    body: string | null;
    data?: Record<string, unknown>;
};

declare function initializeLocalNotifications(rundotGameApi: RundotGameAPI, host: Host): void;

declare class RpcPopupsApi implements PopupsApi {
    private readonly rpcClient;
    constructor(rpcClient: RpcClient);
    showToast(message: string, options?: ShowToastOptions): Promise<boolean>;
    showLikeDialog(): Promise<LikeDialogResult>;
    canShowLikeDialog(): Promise<{
        available: boolean;
    }>;
    showCommentsPanel(): Promise<CommentsPanelResult>;
    canShowCommentsPanel(): Promise<{
        available: boolean;
    }>;
    getLikeState(): Promise<LikeStateResult>;
}
interface ShowToastResponse {
    shown: boolean;
    actionTriggered: boolean;
}

type MockAdType = 'interstitial' | 'rewarded';
interface MockActionSheetItem {
    id?: string;
    label: string;
    icon?: string;
}
interface MockActionSheetOptions {
    title?: string;
    message?: string;
    cancelButtonText?: string;
    disableCancel?: boolean;
}
interface MockOverlay {
    container: HTMLElement;
    elements: OverlayElements;
    adOverlay: HTMLElement;
    actionSheetOverlay: HTMLElement;
    appVisibilityState: 'visible' | 'hidden';
    showAdOverlay: (adType: MockAdType, options?: ShowInterstitialAdOptions | ShowRewardedAdOptions) => Promise<boolean>;
    showActionSheet: (items: MockActionSheetItem[], options?: MockActionSheetOptions) => Promise<string | number | null>;
}
interface OverlayElements {
}

declare class MockPopupsApi implements PopupsApi {
    private readonly overlay;
    constructor(override: MockOverlay);
    showToast(message: string, options?: ShowToastOptions): Promise<boolean>;
    showLikeDialog(): Promise<LikeDialogResult>;
    canShowLikeDialog(): Promise<{
        available: boolean;
    }>;
    showCommentsPanel(): Promise<CommentsPanelResult>;
    canShowCommentsPanel(): Promise<{
        available: boolean;
    }>;
    getLikeState(): Promise<LikeStateResult>;
}

declare function initializePopups(rundotGameApi: RundotGameAPI, host: Host): void;

declare class HostProfileApi implements ProfileApi {
    private readonly rundotGameApi;
    constructor(rundotGameApi: RundotGameAPI);
    getCurrentProfile(): Profile;
}

declare class MockProfileApi implements ProfileApi {
    private readonly rundotGameApi;
    constructor(rundotGameApi: RundotGameAPI);
    getCurrentProfile(): Profile;
}

/**
 * Playground profile API.
 *
 * In playground mode (browser + real backend), profile identity must match the backend's
 * notion of profileId (which is effectively the Firebase Auth UID in this codebase).
 *
 * This implementation reads from `rundotGameApi._profileData`, which is populated by
 * PlaygroundHost when Firebase auth state changes.
 *
 * NOTE: `ProfileApi` is synchronous, so we return a placeholder profile until the
 * user signs in and `_profileData` is available.
 */
declare class PlaygroundProfileApi implements ProfileApi {
    private readonly rundotGameApi;
    constructor(rundotGameApi: RundotGameAPI);
    getCurrentProfile(): Profile;
}

/** @deprecated Use `PlaygroundProfileApi`. */
declare class SandboxProfileApi extends PlaygroundProfileApi {
    constructor(...args: ConstructorParameters<typeof PlaygroundProfileApi>);
}
declare function initializeProfile(rundotGameApi: RundotGameAPI, host: Host): void;

declare class HostDeviceApi implements DeviceApi {
    private readonly rundotGameApi;
    constructor(rundotGameApi: RundotGameAPI);
    getDevice(): DeviceInfo;
}

declare class MockDeviceApi implements DeviceApi {
    private readonly rundotGameApi;
    constructor(rundotGameApi: RundotGameAPI);
    getDevice(): DeviceInfo;
}

declare class HostEnvironmentApi implements EnvironmentApi {
    private readonly rundotGameApi;
    constructor(rundotGameApi: RundotGameAPI);
    getEnvironment(): EnvironmentInfo;
}

declare class MockEnvironmentApi implements EnvironmentApi {
    private readonly rundotGameApi;
    constructor(rundotGameApi: RundotGameAPI);
    getEnvironment(): EnvironmentInfo;
}

/**
 * Host implementation of SystemApi that delegates to device and environment APIs.
 * Acts as a facade combining both system-level information sources.
 */
declare class HostSystemApi implements SystemApi {
    private readonly deviceApi;
    private readonly environmentApi;
    private readonly rundotGameApi;
    private readonly rpcClient;
    constructor(deviceApi: DeviceApi, environmentApi: EnvironmentApi, rundotGameApi: RundotGameAPI, rpcClient: RpcClient);
    getDevice(): DeviceInfo;
    getEnvironment(): EnvironmentInfo;
    getSafeArea(): SafeArea;
    isMobile(): boolean;
    isWeb(): boolean;
    isSteamDesktop(): boolean;
    isSteamDeck(): boolean;
    canAddToHomeScreen(): Promise<boolean>;
    addToHomeScreen(): Promise<AddToHomeScreenResult>;
}

/**
 * Mock implementation of SystemApi for local development.
 * Delegates to mock device and environment APIs.
 */
declare class MockSystemApi implements SystemApi {
    private readonly deviceApi;
    private readonly environmentApi;
    private readonly rundotGameAPI;
    constructor(deviceApi: DeviceApi, environmentApi: EnvironmentApi, rundotGameApi: RundotGameAPI);
    getDevice(): DeviceInfo;
    getEnvironment(): EnvironmentInfo;
    getSafeArea(): SafeArea;
    isMobile(): boolean;
    isWeb(): boolean;
    isSteamDesktop(): boolean;
    isSteamDeck(): boolean;
    canAddToHomeScreen(): Promise<boolean>;
    addToHomeScreen(): Promise<AddToHomeScreenResult>;
}

declare function initializeSystem(rundotGameApi: RundotGameAPI, host: Host): void;

/**
 * Base class for CDN API implementations with shared utility methods
 */
declare abstract class BaseCdnApi implements CdnApi {
    private static readonly AVATAR_ASSETS_PREFIX;
    private static readonly SHARED_LIBS_PREFIX;
    protected abstract getCdnAssetsBaseUrl(): string;
    protected abstract getCdnBaseUrl(): string;
    protected abstract getSharedAssetsCdnBaseUrl(): string;
    /**
     * Remove leading slash from subPath
     */
    protected cleanSubPath(subPath: string): string;
    /**
     * Encode path parts - only encode the filename (last part) to preserve directory structure
     */
    protected encodePathParts(subPath: string): string;
    /**
     * Check if subPath is already a full URL
     */
    protected isFullUrl(subPath: string): boolean;
    /**
     * Normalize base URL to not end with trailing slash
     */
    protected normalizeBaseUrl(url: string): string;
    /**
     * Build full URL with encoding
     */
    protected buildCdnUrl(subPath: string, baseUrl: string): string;
    /**
     * Execute a fetch request with timeout handling
     */
    protected executeFetch(url: string, options?: FetchFromCdnOptions): Promise<Blob>;
    getAssetCdnBaseUrl(): string;
    resolveAssetUrl(subPath: string): Promise<string>;
    resolveAvatarAssetUrl(subPath: string): string;
    resolveSharedLibUrl(subPath: string): string;
    resolveAssetUrls(subPaths: string[]): Promise<AssetUrlResult[]>;
    refreshEntitlements(): Promise<void>;
    abstract fetchAsset(relativePath: string, options?: FetchFromCdnOptions): Promise<Blob>;
    abstract fetchFromCdn(subPath: string, options?: FetchFromCdnOptions): Promise<Blob>;
}

/**
 * CDN API implementation for production/remote environments.
 * Automatically derives the CDN URL from the current window location.
 * Uses RPC to delegate URL resolution to the Client App, with legacy fallback.
 */
declare class HostCdnApi extends BaseCdnApi {
    private static readonly CDN_FOLDER_NAME;
    private static readonly MANIFEST_FILE_PREFIX;
    private static readonly SHARED_ASSETS_CDN_BASE;
    private readonly bucketBaseUrl;
    private readonly gameId;
    private readonly version;
    private readonly shape;
    private readonly rpcClient;
    private manifestCache;
    private manifestPromise;
    constructor(rpcClient: RpcClient);
    protected getCdnAssetsBaseUrl(): string;
    protected getCdnBaseUrl(): string;
    protected getSharedAssetsCdnBaseUrl(): string;
    /**
     * Compute CDN information from the current window location.
     *
     * Accepts both supported URL shapes — see `parseCdnLocationFromUrl` in
     * cdn-url-utils for the layouts. The shape discriminator is propagated
     * to {@link getCdnAssetsBaseUrl}.
     *
     * @throws {Error} If not in browser environment or URL structure doesn't match either expected shape
     */
    private computeCdnInfo;
    /**
     * Fetch directly from CDN root (bucket root).
     * This is the primitive fetch operation that accesses files from the bucket base URL.
     *
     * @param subPath - Path relative to bucket root (e.g., "othergame/bundle.stow", "game123/cdn-assets/file.png")
     * @param options - Optional fetch options including timeout
     * @returns Promise<Blob> - The asset data as a Blob
     *
     * @example
     * // App URL: https://h5-apps.dev.com/development/game123/v1.0.0/
     * // fetchFromCdn("othergame/asset.png")
     * // → https://h5-apps.dev.com/development/othergame/asset.png
     */
    fetchFromCdn(subPath: string, options?: FetchFromCdnOptions): Promise<Blob>;
    /**
     * Resolve a logical asset path to its actual CDN URL.
     * Uses RPC to delegate resolution to the Client App (which handles entitlements
     * and signed URLs). Falls back to legacy manifest-based resolution if the
     * Client App doesn't have the CDN handler.
     */
    resolveAssetUrl(subPath: string): Promise<string>;
    /**
     * Batch resolve multiple asset paths to their CDN URLs.
     * Returns per-path results — a missing entitlement on one path does not fail the batch.
     */
    resolveAssetUrls(subPaths: string[]): Promise<AssetUrlResult[]>;
    /**
     * Refresh the Client App's entitlement cache.
     * Call after a purchase or reward grant so newly acquired entitlements take effect.
     */
    refreshEntitlements(): Promise<void>;
    /**
     * Legacy fallback — fetches manifest directly and builds public URL.
     * Only works for public assets. Protected assets throw a clear error.
     */
    private legacyResolveAssetUrl;
    private legacyResolveAssetUrls;
    private isUnsupportedMessageError;
    /**
     * Fetch an asset using the manifest to resolve the actual hashed filename.
     *
     * @param relativePath - The logical path to the asset (e.g., "/assets/sample.json" or "assets/sample.json")
     * @param options - Optional fetch options including timeout
     * @returns Promise<Blob> - The asset data as a Blob
     * @throws {Error} If the asset is not found in the manifest or the fetch fails
     */
    fetchAsset(relativePath: string, options?: FetchFromCdnOptions): Promise<Blob>;
    /**
     * Get the manifest (legacy fallback), either from cache or by fetching it.
     * Uses promise caching to prevent multiple simultaneous fetches.
     */
    private getManifestLegacy;
    /**
     * Fetch the manifest file from the CDN (legacy fallback).
     * The manifest filename is based on the version: manifest_{version}.json
     */
    private fetchManifestLegacy;
}

/**
 * CDN API implementation for mock/development environments.
 * Serves assets from the public/cdn-assets folder via Vite dev server.
 */
declare class MockCdnApi extends BaseCdnApi {
    private static readonly LEGACY_CDN_PREFIX;
    private static readonly SHARED_ASSETS_CDN_BASE;
    private readonly rundotGameApi;
    constructor(rundotGameApi: RundotGameAPI);
    protected getCdnAssetsBaseUrl(): string;
    protected getCdnBaseUrl(): string;
    protected getSharedAssetsCdnBaseUrl(): string;
    resolveAssetUrl(subPath: string): Promise<string>;
    /**
     * Fetch an asset directly from the bucket root (public folder root in mock mode).
     * This mimics production behavior where cross-game assets are at the bucket root.
     *
     * @param subPath - The path relative to public folder root (e.g., "othergame/bundle.stow")
     * @param options - Optional fetch options including timeout
     * @returns Promise<Blob> - The asset data as a Blob
     *
     * @example
     * // In mock mode with public folder structure:
     * // fetchFromCdn("othergame/bundle.stow")
     * // → othergame/bundle.stow (from public root, not public/cdn-assets)
     */
    fetchFromCdn(subPath: string, options?: FetchFromCdnOptions): Promise<Blob>;
    /**
     * Fetch an asset in mock/development mode.
     * In development, assets are served directly without manifest transformation,
     * so this method fetches from the cdn-assets folder.
     *
     * @param relativePath - The path to the asset (e.g., "/some/asset.png" or "some/asset.png")
     * @param options - Optional fetch options including timeout
     * @returns Promise<Blob> - The asset data as a Blob
     */
    fetchAsset(relativePath: string, options?: FetchFromCdnOptions): Promise<Blob>;
}

/**
 * Shared URL parsing utilities for CDN path detection.
 *
 * Scope: locating a deployed game's assets (which bucket, which gameId, which
 * version) from its own URL — that and nothing else. Whether a RUN host is
 * present is a separate question with a separate answer
 * (`rundot-game-api/hosted-environment.ts`); it is NOT derivable from the URL.
 *
 * Two URL shapes are supported (H5 lockdown R4):
 *
 *   1. Legacy single-host
 *      https://h5-apps.<env>.getreel.com/<env>/<gameId>/<version>/index.html
 *      → bucketBaseUrl = origin + "/<env>"
 *      → gameId        = "<gameId>"
 *      → version       = "<version>"
 *
 *   2. Per-app subdomain
 *      https://<gameId>.h5-apps.<env>.getreel.com/<version>/index.html
 *      https://<gameId>.h5-apps.getreel.com/<version>/index.html   (prod bare apex)
 *      → bucketBaseUrl = origin
 *      → gameId        = host.split('.')[0]
 *      → version       = "<version>"
 *
 * The {@link ParsedCdnLocation.shape} discriminator records which shape
 * matched so callers can branch on it explicitly rather than re-deriving
 * the shape from `bucketBaseUrl` later.
 */
/** Minimum number of non-empty path segments (after stripping filename) required for the legacy hosted CDN URL. */
declare const MIN_CDN_PATH_SEGMENTS = 3;
/** Pattern matching common web page file extensions (used to strip trailing filenames). */
declare const FILE_EXTENSION_PATTERN: RegExp;
/** Per-app subdomain host pattern: `<gameId>.h5-apps.<env>.getreel.com` or `<gameId>.h5-apps.getreel.com` (prod bare apex). */
declare const PER_APP_SUBDOMAIN_HOST_PATTERN: RegExp;
/**
 * Per-app LOCAL host pattern: `<gameId>.h5-apps.localhost[:port]`.
 *
 * WHY this exists: in local docker dev the venus client serves game bytes
 * through the local h5-origin-shim on `http://<gameId>.h5-apps.localhost:10081`
 * (an http origin is required so games may open the local `ws://` room server —
 * the https CDN origin is mixed-content-blocked). Asset resolution must
 * recognize that form, or a game running against the LOCAL client can't locate
 * its own assets — which is exactly a production-shaped bug local dev exists to
 * catch. (Whether a host is present is decided separately, from the client's
 * mount params; this pattern says nothing about that.)
 *
 * WHY it is safe to trust: `.localhost` is IETF-reserved (RFC 6761
 * §6.3) — browsers resolve it to loopback and it cannot be registered as a
 * public domain, so no attacker-controlled page can wear this suffix. The
 * anti-spoof property of the anchored getreel pattern is unchanged. Matched
 * against `url.host`, which carries the port for non-default ports, hence the
 * optional `:port`.
 */
declare const PER_APP_LOCAL_HOST_PATTERN: RegExp;
/** Discriminated union: which of the two supported CDN URL shapes did we parse? */
type CdnUrlShape = 'per-app' | 'legacy';
interface ParsedCdnLocation {
    /** Origin (per-app shape) or origin + bucket path (legacy shape). Never has a trailing slash. */
    bucketBaseUrl: string;
    /** Game / app ID extracted from the URL. */
    gameId: string;
    /** Game version extracted from the URL. */
    version: string;
    /** Which URL shape this came from. Callers branch on this rather than re-detecting. */
    shape: CdnUrlShape;
}
/**
 * Return true when {@link host} matches the per-app subdomain layout
 * `<gameId>.h5-apps.<env>.getreel.com` (or `<gameId>.h5-apps.getreel.com` for
 * the prod bare apex), or the local docker-shim form
 * `<gameId>.h5-apps.localhost[:port]`. The patterns are anchored at both ends
 * so attacker suffixes like `evil.h5-apps.getreel.com.attacker.tld` do NOT
 * match (and `.localhost` is loopback-only per RFC 6761, so it cannot be an
 * attacker suffix either).
 *
 * Returns false for the legacy single-host (`h5-apps.<env>.getreel.com`),
 * bare localhost / dev-server hosts, and anything else.
 */
declare function isPerAppSubdomain(host: string): boolean;
/**
 * Parse the current `window.location` into cleaned path segments suitable
 * for legacy-shape CDN URL extraction. Kept for back-compat with external
 * consumers (re-exported from the SDK barrel).
 *
 * Note: for new code, prefer {@link parseCdnLocationFromWindow}, which
 * handles both legacy and per-app subdomain shapes.
 */
declare function parseCdnPathSegments(): string[] | null;
/**
 * Parse a {@link URL} into a {@link ParsedCdnLocation}, detecting whether
 * it is a per-app subdomain or a legacy single-host CDN URL.
 *
 * Returns `null` if neither shape matches (e.g., localhost dev URLs, or
 * a host that matches per-app but has no version segment).
 */
declare function parseCdnLocationFromUrl(url: URL): ParsedCdnLocation | null;
/**
 * Parse the current `window.location` into a {@link ParsedCdnLocation}.
 * Returns `null` outside a browser environment, on URL parse failure, or
 * when the location matches neither shape.
 */
declare function parseCdnLocationFromWindow(): ParsedCdnLocation | null;

declare function initializeCdn(rundotGameApi: RundotGameAPI, host: Host): void;

declare class HostTimeApi implements TimeApi {
    private readonly rpcClient;
    private readonly rundotGameApi;
    constructor(rpcClient: RpcClient, rundotGameApi: RundotGameAPI);
    requestTimeAsync(): Promise<ServerTimeData>;
    formatTime(timestamp: number, options?: any): string;
    formatNumber(value: number, options?: Intl.NumberFormatOptions): string;
    getFutureTimeAsync(options?: GetFutureTimeOptions): Promise<number>;
}

declare function isPacificDaylightTime(date: Date): boolean;

declare class MockTimeApi implements TimeApi {
    private readonly rundotGameApi;
    constructor(rundotGameApi: RundotGameAPI);
    formatNumber(value: number, options?: Intl.NumberFormatOptions): string;
    formatTime(timestamp: number, options?: any): string;
    getFutureTimeAsync(options?: GetFutureTimeOptions): Promise<number>;
    requestTimeAsync(): Promise<ServerTimeData>;
}

declare function initializeTime(rundotGameApi: RundotGameAPI, host: Host): void;

declare class RpcAiApi implements AiApi {
    private readonly rpcClient;
    constructor(rpcClient: RpcClient);
    requestChatCompletionAsync(request: AiChatCompletionRequest): Promise<AiChatCompletionData>;
    requestPromptCompletionAsync(request: PromptCompletionRequest): Promise<AiChatCompletionData>;
    getAvailableCompletionModels(): Promise<Array<string>>;
    requestChatCompletionStreamAsync(request: AiChatCompletionRequest, options?: AiChatCompletionStreamOptions): AsyncIterable<AiChatCompletionStreamChunk>;
    requestPromptCompletionStreamAsync(request: PromptCompletionRequest, options?: AiChatCompletionStreamOptions): AsyncIterable<AiChatCompletionStreamChunk>;
    /**
     * Shared stream machinery for both open-mode and templated streams: START
     * round-trip, CHUNK/DONE/ERROR notification fan-in, ABORT on cancel, and
     * credits-exhaustion re-typing. The wire request is the only thing that
     * differs between the two public methods.
     */
    private openStream;
}

declare class MockAiApi implements AiApi {
    requestChatCompletionAsync(request: AiChatCompletionRequest): Promise<AiChatCompletionData>;
    getAvailableCompletionModels(): Promise<Array<string>>;
    /**
     * Canned stream so agent loops and tool flows can be exercised offline.
     *
     * Unlike the non-streaming sibling this does NOT throw: a streaming consumer
     * that cannot get a first chunk cannot be developed against the mock at all.
     * The text is prefixed `[mock]` so it is never mistaken for a real response.
     */
    requestChatCompletionStreamAsync(request: AiChatCompletionRequest, options?: AiChatCompletionStreamOptions): AsyncIterable<AiChatCompletionStreamChunk>;
    requestPromptCompletionAsync(_request: PromptCompletionRequest): Promise<AiChatCompletionData>;
    requestPromptCompletionStreamAsync(_request: PromptCompletionRequest, _options?: AiChatCompletionStreamOptions): AsyncIterable<AiChatCompletionStreamChunk>;
    /** Warn-now, throw-on-first-`next()` iterable shared by both mock stream methods. */
    private mockUnavailableStream;
    /**
     * One tool call on the first pass, then text once a tool result comes back,
     * so a tool loop terminates instead of calling the same tool forever.
     */
    private mockCompletionStream;
    /** True once the conversation carries a tool result, i.e. a tool already ran. */
    private static hasToolResult;
    /**
     * Deterministic canned response when the request supplies tools. Always
     * invokes the first tool in the list with an empty argument object — keeps
     * SDK consumers' tool-flow unit tests stable without a real provider.
     */
    private buildMockToolCallResponse;
}

/**
 * @deprecated RundotGameAPI.ai is deprecated. Use RundotGameAPI.textGen instead.
 *
 * Wraps the underlying textGen implementation with a warn-once deprecation
 * notice so existing games keep working while migrating.
 */
declare function initializeAi(rundotGameApi: RundotGameAPI, _host: Host): void;

declare class RpcHapticsApi implements HapticsApi {
    private readonly rpcClient;
    constructor(rpcClient: RpcClient);
    triggerHapticAsync(style: HapticFeedbackStyle): Promise<void>;
}

declare class MockHapticsApi implements HapticsApi {
    private readonly rundotGameApi;
    constructor(rundotGameApi: RundotGameAPI);
    triggerHapticAsync(style: HapticFeedbackStyle): Promise<void>;
}

declare function initializeHaptics(rundotGameApi: RundotGameAPI, host: Host): void;

/**
 * Common reader contract shared by Tier 1 (`WebGamepadReader`) and the Tier-2
 * test/native fakes. `RpcGamepadApi` drives whichever reader is active.
 */
interface GamepadReader {
    start(onSnapshot: (snapshot: GamepadSnapshot) => void, onConnected: (event: GamepadConnectionEvent) => void, onDisconnected: (event: GamepadConnectionEvent) => void): void;
    read(): GamepadSnapshot[];
    stop(): void;
}
/**
 * Maps a browser standard-mapping `Gamepad` to the normalized `GamepadSnapshot`.
 * Bounds-checks `buttons`/`axes` so a short/garbage array yields zeros rather
 * than throwing (MUST-26). `sampledAt` stamps both timestamps equal — Tier 1
 * adds no transport latency.
 */
declare function normalizeStandardGamepad(pad: Gamepad, sampledAt: number): GamepadSnapshot;
/**
 * Tier 1 reader: reads `navigator.getGamepads()` live on the game's own rAF and
 * surfaces connect/disconnect from the game context's window events. Zero added
 * latency — never crosses the host bus.
 */
declare class WebGamepadReader implements GamepadReader {
    private rafId;
    private connectedListener;
    private disconnectedListener;
    private started;
    read(): GamepadSnapshot[];
    start(onSnapshot: (snapshot: GamepadSnapshot) => void, onConnected: (event: GamepadConnectionEvent) => void, onDisconnected: (event: GamepadConnectionEvent) => void): void;
    stop(): void;
}

/**
 * Minimal RPC seam consumed by `RpcGamepadApi`. Structurally satisfied by the
 * SDK's `RpcClient`. `createWebGamepadReader` is an optional injection seam for
 * tests / non-browser hosts; production omits it and the real
 * `WebGamepadReader` is used.
 */
interface GamepadRpc {
    call<T>(method: string, args?: unknown, timeoutMs?: number): Promise<T>;
    onNotification<T>(id: string, callback: (payload: T) => void): Subscription;
    notify(method: string, args?: unknown): void;
    createWebGamepadReader?(): GamepadReader;
}
interface RpcGamepadApiOptions {
    debug?: boolean;
    timeoutMs?: number;
    /** Defaults to true — the handshake fires on construction (the SDK default).
     * The remote host passes `false` and calls `subscribe()` once its transport
     * is live. */
    autoSubscribe?: boolean;
    /** How often (ms) the `gamepad.latency` telemetry is emitted while `debug` is on
     * (MUST-19). Off entirely when `debug` is false. */
    latencyEmitIntervalMs?: number;
}
declare class RpcGamepadApi implements GamepadApi {
    private readonly rpc;
    private supported;
    private mode;
    private tornDown;
    /** Set once the handshake settles to an unsupported state. */
    private serveDisabled;
    private readonly cache;
    private readonly lastSourceByIndex;
    private readonly connectedSubscribers;
    private readonly disconnectedSubscribers;
    private readonly notificationSubscriptions;
    private readonly latency;
    private reader;
    private handshakePromise;
    private pagehideListener;
    private latencyEmitTimer;
    private readonly timeoutMs;
    constructor(rpc: GamepadRpc, options?: RpcGamepadApiOptions);
    /** Issues the awaited `GAMEPAD_SUBSCRIBE` handshake. The host's response is the
     * sole tier authority (MUST-22/23). */
    subscribe(): Promise<void>;
    /** Resolves once the handshake settles (success or failure). Never rejects. */
    ready(): Promise<void>;
    isSupported(): boolean;
    getGamepads(): GamepadSnapshot[];
    onConnected(callback: (event: GamepadConnectionEvent) => void): Subscription;
    onDisconnected(callback: (event: GamepadConnectionEvent) => void): Subscription;
    readonly __debug: {
        getLatencyStats: () => GamepadLatencyStats;
    };
    private emitLatency;
    teardown(): void;
    private applyHandshake;
    private failHandshake;
    private markUnsupported;
    private stopReader;
    private handleSnapshot;
    private handleConnected;
    private handleDisconnected;
    private dispatchConnected;
    private dispatchDisconnected;
    private dispatchConnection;
    private coerceSnapshot;
    private coerceConnection;
    /** Tier 2 spans multiple providers; trust the host's stamped source, else
     * reuse the last valid source for this index, else any cached pad's source,
     * else an explicitly-arbitrary guard (MUST-26). */
    private resolveSource;
}

/**
 * Mock/playground input: a local Tier-1 passthrough. When the browser exposes the
 * Web Gamepad API the dev surface reads real controllers; otherwise it reports
 * unsupported with an empty pad list.
 */
declare class MockGamepadApi implements GamepadApi {
    private readonly reader;
    private readonly latency;
    private readonly connectedSubscribers;
    private readonly disconnectedSubscribers;
    private started;
    /** No handshake on the local/mock tier — capability is known synchronously. */
    ready(): Promise<void>;
    isSupported(): boolean;
    getGamepads(): GamepadSnapshot[];
    onConnected(callback: (event: GamepadConnectionEvent) => void): Subscription;
    onDisconnected(callback: (event: GamepadConnectionEvent) => void): Subscription;
    readonly __debug: {
        getLatencyStats: () => GamepadLatencyStats;
    };
    private ensureStarted;
}

interface LatencyTrackerOptions {
    windowSize: number;
    enabled: boolean;
}
/**
 * Rolling-window latency stats (p50/p95/max) computed from
 * `deliveredTimestamp - sourceTimestamp` per snapshot. A no-op when disabled so
 * production builds carry zero overhead behind the input debug flag.
 */
declare class LatencyTracker {
    private readonly opts;
    private readonly samples;
    constructor(opts: LatencyTrackerOptions);
    add(sourceTimestamp: number, deliveredTimestamp: number): void;
    getStats(): GamepadLatencyStats;
}

declare function initializeGamepad(rundotGameApiInstance: RundotGameAPI, host: Host): void;

declare class RpcFeaturesApi implements FeaturesApi {
    private readonly rpcClient;
    constructor(rcpClient: RpcClient);
    getExperiment(experimentName: string): Promise<Experiment | null>;
    getFeatureFlag(flagName: string): Promise<boolean>;
    getFeatureGate(gateName: string): Promise<boolean>;
}

declare class MockFeaturesApi implements FeaturesApi {
    getExperiment(experimentName: string): Promise<Experiment | null>;
    getFeatureFlag(flagName: string): Promise<boolean>;
    getFeatureGate(gateName: string): Promise<boolean>;
}

declare function initializeFeaturesApi(rundotGameApi: RundotGameAPI, host: Host): void;

interface LiveOpsHooks {
    /** Bucketing unit — the current profile id. Undefined = experiments skipped. */
    getUnitId?: () => string | undefined;
    /** Fired once per (unitId, experimentId, variantId) per SDK lifetime, after resolution. */
    onExposure?: (assignment: ExperimentAssignment, configVersion: string) => void;
}

declare class RpcLiveOpsApi implements LiveOpsApi {
    private cache;
    constructor(rpcClient: RpcClient, hooks?: LiveOpsHooks);
    clearConfigCache(): void;
    getConfigAsync(options?: {
        maxAgeMs?: number;
    }): Promise<LiveOpsConfigResult>;
    getRawConfigAsync(options?: {
        maxAgeMs?: number;
    }): Promise<LiveOpsRawConfig>;
}

/**
 * Mock LiveOps — serves the local `rundot/liveops.config.json` `client` section so
 * you can develop against LiveOps without playground sign-in or a backend. The
 * section comes from either an explicit `configure()` (tests) or a live provider
 * MockHost wires to `rundotGameApi._mock.liveops.client` (the Vite plugin injects
 * that from the on-disk file; edits hot-reload). Read live on every call — the
 * same pure resolver as prod, anchored to `Date.now()`, so scheduled overrides
 * flip on schedule.
 */
declare class MockLiveOpsApi implements LiveOpsApi {
    private getClient?;
    private hooks?;
    private explicitClient;
    private configured;
    private deduper;
    constructor(getClient?: (() => LiveOpsSection | undefined) | undefined, hooks?: LiveOpsHooks | undefined);
    /** Explicit override (wins over the provider). Used by tests and programmatic setup. */
    configure(config: {
        client?: LiveOpsSection;
    }): void;
    clearConfigCache(): void;
    private resolveClient;
    getConfigAsync(): Promise<LiveOpsConfigResult>;
    getRawConfigAsync(): Promise<LiveOpsRawConfig>;
}

declare function initializeLiveOps(rundotGameApi: RundotGameAPI, host: Host): void;

declare class MockLifecycleApi implements LifecycleApi {
    private pauseCallbacks;
    private resumeCallbacks;
    private awakeCallbacks;
    private sleepCallbacks;
    private quitCallbacks;
    private backButtonCallbacks;
    private identityChangedCallbacks;
    onSleep(callback: SleepCallback): Subscription;
    onAwake(callback: AwakeCallback): Subscription;
    onPause(callback: PauseCallback): Subscription;
    onResume(callback: ResumeCallback): Subscription;
    onQuit(callback: QuitCallback): Subscription;
    triggerPauseCallbacks(): void;
    triggerResumeCallbacks(): void;
    triggerAwakeCallbacks(): void;
    triggerSleepCallbacks(): void;
    triggerQuitCallbacks(): void;
    onBackButton(callback: BackButtonCallback): Subscription;
    triggerBackButtonCallbacks(): void;
    onIdentityChanged(callback: IdentityChangedCallback): Subscription;
    triggerIdentityChangedCallbacks(event: IdentityChangedEvent): void;
}

declare class RpcLifecycleApi implements LifecycleApi {
    private readonly rpcClient;
    private backButtonHandlerRegistered;
    constructor(rpcClient: RpcClient);
    onQuit(callback: QuitCallback): Subscription;
    onSleep(callback: SleepCallback): Subscription;
    onAwake(callback: AwakeCallback): Subscription;
    onResume(callback: ResumeCallback): Subscription;
    onPause(callback: PauseCallback): Subscription;
    onIdentityChanged(callback: IdentityChangedCallback): Subscription;
    onBackButton(callback: BackButtonCallback): Subscription;
}

/**
 * Subscribes an internal handler that overwrites `rundotGameApi._profileData`
 * with the canonical profile the instant an IDENTITY_CHANGED notification
 * arrives. RemoteHost MUST call this during initialization, before any game
 * code subscribes via `lifecycle.onIdentityChanged`, so that the replica is
 * already up to date when game callbacks run (RpcClient fans out to
 * subscribers in registration order). A game reading `api.profile` inside its
 * own handler then sees the new identity, not the stale one.
 */
declare function wireIdentityReplica(rpcClient: RpcClient, rundotGameApi: RundotGameAPI): Subscription;

declare function initializeLifecycleApi(rundotGameApi: RundotGameAPI, host: Host): void;

declare class RpcSimulationApi implements SimulationApi {
    private readonly rpcClient;
    private _simulationConfig;
    private readonly subscriptionCallbacks;
    constructor(rpcClient: RpcClient);
    isEnabled(): boolean;
    validateSlotAssignmentAsync(containerId: string, slotId: string, itemId: string): Promise<SimulationSlotValidationResult>;
    subscribeAsync(options: SimulationSubscribeOptions): Promise<() => void>;
    executeBatchOperationsAsync(operations: Array<SimulationBatchOperation>, validateOnly?: boolean): Promise<SimulationBatchOperationsResult>;
    getAvailableItemsAsync(containerId: string, slotId: string): Promise<SimulationAvailableItem[]>;
    calculatePowerPreviewAsync(containerId: string, slotId: string, candidateItemId: string): Promise<SimulationPowerPreview>;
    assignItemToSlotAsync(containerId: string, slotId: string, itemId: string): Promise<SimulationSlotMutationResult>;
    removeItemFromSlotAsync(containerId: string, slotId: string): Promise<SimulationSlotMutationResult>;
    getSlotContainersAsync(): Promise<SimulationSlotContainer[]>;
    getSlotAssignmentsAsync(containerId: string): Promise<SimulationAssignment[]>;
    getStateAsync(roomId?: string): Promise<SimulationState>;
    getConfigAsync(roomId?: string): Promise<RundotGameSimulationConfig>;
    executeRecipeAsync(recipeId: string, inputs?: Record<string, unknown>, options?: ExecuteRecipeOptions): Promise<ExecuteRecipeResponse>;
    collectRecipeAsync(runId: string): Promise<CollectRecipeResult>;
    resetStateAsync(options?: ResetStateOptions): Promise<ResetStateResult>;
    getActiveRunsAsync(options?: GetActiveRunsOptions): Promise<SimulationRunSummary[]>;
    executeScopedRecipeAsync(recipeId: string, entity: string, inputs?: Record<string, unknown>, options?: ExecuteScopedRecipeOptions): Promise<ExecuteScopedRecipeResult>;
    getAvailableRecipesAsync(options?: GetAvailableRecipesOptions): Promise<GetAvailableRecipesResult>;
    getRecipeRequirementsAsync(recipe: Recipe): Promise<RecipeRequirementResult>;
    getBatchRecipeRequirementsAsync(recipes: Recipe[]): Promise<GetBatchRecipeRequirements>;
    triggerRecipeChainAsync(recipeId: string, options?: TriggerRecipeChainOptions): Promise<ExecuteRecipeResponse>;
    getEntityMetadataAsync(entityId: string): Promise<Record<string, unknown>>;
    resolveFieldValueAsync(entityId: string, fieldPath: string, entity?: string): Promise<unknown>;
    private handleSimulationUpdate;
    private ensureValidSubscribeOptions;
}

declare function initializeSimulation(rundotGameApi: RundotGameAPI, host: Host): void;

interface RoomNotificationSubscriptionState {
    data: Record<string, Array<(event: RoomDataUpdate) => void>>;
    messages: Record<string, Array<(event: RoomMessageEvent) => void>>;
    gameEvents: Record<string, Array<(event: ProposedMoveEvent) => void>>;
}
/**
 * Set up room notification listeners using the transport's onRundotGameMessage hook.
 * This routes host-sent room notifications (H5_ROOM_DATA_UPDATED, etc.) to the
 * callbacks registered in the RoomsApi instance.
 * @param transport The RUN.world transport to listen for messages
 * @param getSubscriptions Function to retrieve subscription state from RoomsApi
 */
declare function setupRoomNotifications(transport: RundotGameTransport, getSubscriptions: () => RoomNotificationSubscriptionState): Subscription;

declare class RpcRoomsApi implements RoomsApi {
    private readonly rpcClient;
    private readonly subscriptions;
    constructor(rpcClient: RpcClient);
    /**
     * Get the subscription state for external access (used by setupRoomNotifications)
     */
    getSubscriptions(): RoomNotificationSubscriptionState;
    /**
     * Set up room notification routing from the transport
     */
    setupNotifications(transport: RundotGameTransport): void;
    createRoomAsync(options: CreateRoomOptions): Promise<RundotGameRoom>;
    joinOrCreateRoomAsync(options: JoinOrCreateRoomOptions): Promise<JoinOrCreateResult>;
    joinRoomByCodeAsync(roomCode: string): Promise<RundotGameRoom>;
    getUserRoomsAsync(options?: ListRoomsOptions): Promise<RundotGameRoom[]>;
    /** @deprecated Rooms V1 is unsupported. Hold state in your realtime `GameRoom` class. */
    updateRoomDataAsync(room: RundotGameRoom, updates: Record<string, unknown>, options?: UpdateRoomDataOptions): Promise<void>;
    getRoomDataAsync(room: RundotGameRoom): Promise<Record<string, unknown>>;
    sendRoomMessageAsync(rundotGameRoom: RundotGameRoom, request: RoomMessageRequest): Promise<string>;
    leaveRoomAsync(room: RundotGameRoom): Promise<void>;
    kickPlayerAsync(room: RundotGameRoom, targetProfileId: string, options?: {
        reason?: string;
    }): Promise<void>;
    startRoomGameAsync(room: RundotGameRoom, options?: StartRoomGameOptions): Promise<void>;
    proposeMoveAsync(room: RundotGameRoom, proposalPayload: ProposeMoveRequest): Promise<ProposeMoveResult>;
    validateMoveAsync(_room: RundotGameRoom, moveId: string, verdict: ValidateMoveVerdict): Promise<ValidateMoveResult>;
    subscribeAsync(room: RundotGameRoom, options?: RoomSubscriptionOptions): Promise<() => void>;
}

declare function initializeRoomsApi(rundotGameApi: RundotGameAPI, host: Host): void;

declare class MockLoggingApi implements LoggingApi {
    logDebug(message?: any, ...args: any[]): void;
    logError(message: string, ...args: any[]): void;
}

declare class RpcLoggingApi implements LoggingApi {
    private readonly host;
    private readonly rpcClient;
    constructor(host: Host, rpcClient: RpcClient);
    logDebug(message: string, ...args: any[]): void;
    logError(message: string, ...args: any[]): void;
    private buildMessage;
    private toStringArg;
}

declare function initializeLoggingApi(rundotGameApi: RundotGameAPI, host: Host): void;

declare class RpcIapApi implements IapApi {
    private readonly rpcClient;
    constructor(rpcClient: RpcClient);
    getHardCurrencyBalance(): Promise<number>;
    spendCurrency(productId: string, cost: number, options?: SpendCurrencyOptions): Promise<SpendCurrencyResult>;
    /**
      * gets the subscription packages for a given tier. If no tier is provided, returns all subscription
      * packages for all tiers
      */
    getSubscriptions(tier?: SubscriptionTier): Promise<RunSubscriptionsResponse>;
    /**
      * triggers the checkout for a given subscription, identified by tier and interval.
      * Rejects with a `RundotApiError` (`UNSUPPORTED_SUBSCRIPTION_INTERVAL`) before
      * any checkout when the tier does not offer that interval — LITE is weekly-only.
      * @example
      * // will trigger the checkout for the weekly CORE subscription
      * await purchaseSubscription('CORE', 'weekly')
      */
    purchaseSubscription(tier: SubscriptionTier, interval: SubscriptionInterval): Promise<PurchaseSubscriptionResponse>;
    /**
      * Check if a user has a given subscription tier. If the user has a higher
      * subscription tier than the one being checked, this returns true.
      *
      * @example
      * // Returns true if the user has CORE, PLUS, PRIME, or ULTIMATE
      * await isUserSubscribed('CORE');
      *
      * // Returns true only if the user has ULTIMATE
      * await isUserSubscribed('ULTIMATE');
      */
    isUserSubscribed(tier: SubscriptionTier): Promise<boolean>;
    openStore(): Promise<OpenStoreResult>;
    getCurrencyIcon(options?: GetCurrencyIconOptions): Promise<LoadEmbeddedAssetsResponse>;
    hasUserMadePurchase(): Promise<boolean>;
    listDirectPurchaseSkus(gameId: string): Promise<DirectPurchaseSku[]>;
}

declare class MockSharedAssetsApi implements SharedAssetsApi {
    private readonly rundotGameApi;
    constructor(rundotGameApi: RundotGameAPI);
    loadAssetsBundle(game: string, bundleKey: string, fileType?: string): Promise<ArrayBuffer>;
}

type ModuleSpecifierMatch = 'exact' | 'prefix';
interface ModuleSpecifierConfig {
    match: ModuleSpecifierMatch;
    value: string;
    behavior?: 'default' | 'react-jsx-runtime' | 'react-jsx-dev-runtime' | 'namespace';
}
interface EmbeddedLibraryDefinition {
    libraryKey: string;
    assetKey: string;
    packageName: string;
    version: string;
    globalVar: string;
    cdnPath: string;
    moduleSpecifiers: ModuleSpecifierConfig[];
    loadStage: number;
    enabled: boolean;
    sha384: string;
}
declare const DEFAULT_SHARED_LIB_CDN_BASE = "https://venus-static-01293ak.web.app/libs";
declare const EMBEDDED_LIBRARIES: EmbeddedLibraryDefinition[];
declare const EMBEDDED_LIBRARY_BY_KEY: Record<string, EmbeddedLibraryDefinition>;
declare const MODULE_TO_LIBRARY_SPECIFIERS: {
    libraryKey: string;
    match: ModuleSpecifierMatch;
    value: string;
    behavior?: "default" | "react-jsx-runtime" | "react-jsx-dev-runtime" | "namespace";
}[];
/**
 * Get library definition by key, throwing if not found.
 * @param libraryKey - Library key (e.g., 'phaser@3.90.0')
 * @returns Library definition
 * @throws Error if library key is not found
 */
declare function getLibraryDefinition(libraryKey: string): EmbeddedLibraryDefinition;

/**
 * Base64 encoding/decoding utilities for embedded assets and libraries.
 *
 * These utilities handle base64 data received from the RUN.world host via RPC,
 * converting it to ArrayBuffer or UTF-8 strings with appropriate fallbacks
 * for different JavaScript environments.
 */
/**
 * Convert base64 string to ArrayBuffer.
 * Uses native atob() when available for best performance.
 */
declare function base64ToArrayBuffer(base64: string): ArrayBuffer;
/**
 * Decode a base64 chunk directly into `target` starting at `offset`.
 *
 * Returns the number of bytes actually decoded. Callers assembling a stream
 * of chunks MUST advance their cursor by this return value, not by the
 * length they requested: native file reads (Android `skip`/`read`) may
 * legally return short, and the host encodes only the bytes it read.
 */
declare function base64DecodeInto(base64: string, target: Uint8Array, offset: number): number;
/**
 * Convert base64 string to UTF-8 string.
 * Tries multiple decoding strategies for maximum compatibility.
 */
declare function base64ToUtf8(base64: string): string;

/**
 * A source of base64-encoded byte slices of a fixed-size asset.
 * `readChunk(position, length)` must return the base64 encoding of the bytes
 * it actually read starting at `position` — which may be FEWER than `length`
 * (short read), but never more.
 */
interface ChunkedAssetSource {
    totalBytes: number;
    chunkBytes: number;
    readChunk: (position: number, length: number) => Promise<string>;
}
/**
 * Assemble an asset by pulling it chunk-by-chunk from `source`.
 *
 * Invariants (all structural, see venus issue #2749):
 * - The cursor advances by the ACTUAL decoded byte count of each chunk, so
 *   short reads self-heal: the next request re-starts at the real cursor.
 * - `length` is always clamped to `totalBytes - position` and a request is
 *   never issued at `position >= totalBytes` (Android's native read at EOF
 *   returns -1 and its base64 encoder throws on a negative count).
 * - A zero-byte chunk cannot advance the cursor and would loop forever, so
 *   it is an error.
 * - Each chunk gets exactly ONE retry; a second failure aborts the whole
 *   transfer. Callers decide what a failed transfer falls back to.
 */
declare function readAssetInChunks(source: ChunkedAssetSource): Promise<ArrayBuffer>;

declare class MockIapApi implements IapApi {
    private _hardCurrency;
    get hardCurrency(): number;
    set hardCurrency(value: number);
    spendCurrency(productId: string, cost: number, options?: SpendCurrencyOptions): Promise<SpendCurrencyResult>;
    getHardCurrencyBalance(): Promise<number>;
    openStore(): Promise<OpenStoreResult>;
    /**
     * One embedded blob stands in for all eight variant/tier assets — inlining
     * the real set would bloat the SDK bundle. Options are logged, not honoured,
     * so mock output never matches a `mono` request visually.
     */
    getCurrencyIcon(options?: GetCurrencyIconOptions): Promise<LoadEmbeddedAssetsResponse>;
    getSubscriptions(tier?: SubscriptionTier): Promise<RunSubscriptionsResponse>;
    purchaseSubscription(tier: SubscriptionTier, interval: SubscriptionInterval): Promise<PurchaseSubscriptionResponse>;
    isUserSubscribed(tier: SubscriptionTier): Promise<boolean>;
    hasUserMadePurchase(): Promise<boolean>;
    listDirectPurchaseSkus(gameId: string): Promise<DirectPurchaseSku[]>;
}

declare function initializeIap(rundotGameApiInstance: RundotGameAPI, host: Host): void;

declare class RpcCreditsApi implements CreditsApi {
    private readonly rpcClient;
    private autoPaywallOnExhaustion;
    private autoRetryOnPurchase;
    constructor(rpcClient: RpcClient);
    getBillingContext(): Promise<CreditsBillingContext>;
    getBalance(): Promise<CreditBalance>;
    getSubscription(): Promise<CreditSubscription>;
    getPlans(): Promise<CreditPlansCatalog>;
    estimateGenerationCost(request: GenerationCostEstimateRequest): Promise<GenerationCostEstimate>;
    openPaywall(options?: OpenPaywallOptions): Promise<CreditsPurchaseResult>;
    setAutoPaywallOnExhaustion(enabled: boolean): void;
    getAutoPaywallOnExhaustion(): boolean;
    setAutoRetryOnPurchase(enabled: boolean): void;
    getAutoRetryOnPurchase(): boolean;
    onBalanceChanged(listener: (balance: CreditBalance) => void): Unsubscribe;
    private pushConfig;
}

declare class MockCreditsApi implements CreditsApi {
    private autoPaywallOnExhaustion;
    private autoRetryOnPurchase;
    private balance;
    getBillingContext(): Promise<CreditsBillingContext>;
    getBalance(): Promise<CreditBalance>;
    getSubscription(): Promise<CreditSubscription>;
    getPlans(): Promise<CreditPlansCatalog>;
    estimateGenerationCost(_request: GenerationCostEstimateRequest): Promise<GenerationCostEstimate>;
    openPaywall(options?: OpenPaywallOptions): Promise<CreditsPurchaseResult>;
    setAutoPaywallOnExhaustion(enabled: boolean): void;
    getAutoPaywallOnExhaustion(): boolean;
    setAutoRetryOnPurchase(enabled: boolean): void;
    getAutoRetryOnPurchase(): boolean;
    onBalanceChanged(_listener: (balance: CreditBalance) => void): Unsubscribe;
}

declare class HttpCreditsApi extends MockCreditsApi {
    private readonly appId;
    private readonly deps;
    constructor(appId: string, deps: BackendHttpDeps);
    estimateGenerationCost(request: GenerationCostEstimateRequest): Promise<GenerationCostEstimate>;
}

declare function initializeCredits(rundotGameApiInstance: RundotGameAPI, host: Host): void;

declare class RpcLeaderboardApi implements LeaderboardApi {
    private readonly rpcClient;
    /** Cache of score tokens for automatic hash computation */
    private tokenCache;
    constructor(rpcClient: RpcClient);
    /**
     * Create a score token for submitting a score.
     * Token is cached for automatic hash computation if score sealing is enabled.
     *
     * @param mode - Optional game mode
     * @returns Score token with sealing data if enabled
     */
    createScoreToken(mode?: string): Promise<ScoreToken>;
    /**
     * Submit a score to the leaderboard.
     * Automatically computes hash if score sealing is enabled and token was created via createScoreToken().
     *
     * @param params - Score submission parameters
     * @returns Submission result with acceptance status and rank
     * @throws Error if token not found in cache
     */
    submitScore(params: SubmitScoreParams): Promise<SubmitScoreResult>;
    getPagedScores(options?: GetPagedScoresOptions): Promise<PagedScoresResponse>;
    getMyRank(options?: PlayerRankOptions): Promise<PlayerRankResult>;
    getPodiumScores(options?: GetPodiumScoresOptions): Promise<PodiumScoresResponse>;
}

declare class MockLeaderboardApi implements LeaderboardApi {
    private tokens;
    /** Cache of score tokens for automatic hash computation */
    private tokenCache;
    private entriesByMode;
    private tokenCounter;
    private enableScoreSealing;
    private scoreSealingSecret;
    constructor(options?: {
        enableScoreSealing?: boolean;
        scoreSealingSecret?: string;
    });
    /**
     * Configure mock leaderboard settings
     *
     * @param options - Configuration options
     */
    configure(options: {
        enableScoreSealing?: boolean;
        scoreSealingSecret?: string;
    }): void;
    private generateNonce;
    private getModeKey;
    private getEntriesForMode;
    /**
     * Create a mock score token for testing.
     * Token is cached for automatic hash computation if score sealing is enabled.
     *
     * @param mode - Optional game mode
     * @returns Score token with sealing data if enabled
     */
    createScoreToken(mode?: string): Promise<ScoreToken>;
    /**
     * Submit a mock score to the leaderboard.
     * Automatically computes hash if score sealing is enabled and token was created via createScoreToken().
     *
     * @param params - Score submission parameters
     * @returns Submission result with acceptance status and rank
     * @throws Error if token not found in cache or validation fails
     */
    submitScore(params: SubmitScoreParams): Promise<SubmitScoreResult>;
    getPagedScores(options?: GetPagedScoresOptions): Promise<PagedScoresResponse>;
    getMyRank(_options?: PlayerRankOptions): Promise<PlayerRankResult>;
    getPodiumScores(options?: GetPodiumScoresOptions): Promise<PodiumScoresResponse>;
}

/**
 * Hash algorithm used for score sealing
 */
declare const HASH_ALGORITHM_WEB_CRYPTO = "SHA-256";
declare const HASH_ALGORITHM_NODE = "sha256";
/**
 * Compute HMAC-SHA256 hash for score sealing.
 * Hash always includes: score, duration, token
 * Matches server-side hash computation exactly.
 *
 * @param score - The score value
 * @param duration - Duration in seconds
 * @param token - Score token
 * @param sealingNonce - One-time nonce for this submission
 * @param sealingSecret - Secret key for HMAC
 * @returns Hex-encoded hash string
 */
declare function computeScoreHash(score: number, duration: number, token: string, sealingNonce: string, sealingSecret: string): Promise<string>;
/**
 * Map a submit-score transport error to a typed, non-throwing
 * {@link SubmitScoreResult} the game can branch on — or `undefined` when the
 * error is a genuine failure that should still throw. Shared by both the RPC and
 * HTTP leaderboard impls so every submission surface resolves the same
 * pending → ranked/rejected outcomes (identity change, replay rejection) instead
 * of one path throwing an opaque error.
 */
declare function mapSubmitScoreFailure(error: unknown): SubmitScoreResult | undefined;

declare function initializeLeaderboard(rundotGameApiInstance: RundotGameAPI, host: Host): void;

declare class MockPreloaderApi implements PreloaderApi {
    showLoadScreen(): Promise<void>;
    hideLoadScreen(): Promise<void>;
    setLoaderText(text: string): Promise<void>;
    setLoaderProgress(progress: number): Promise<void>;
}

declare class RpcPreloaderApi implements PreloaderApi {
    private readonly rpcClient;
    constructor(rpcClient: RpcClient);
    showLoadScreen(): Promise<void>;
    hideLoadScreen(): Promise<void>;
    setLoaderText(text: string): Promise<void>;
    setLoaderProgress(progress: number): Promise<void>;
}

declare function initializePreloader(rundotGameApi: RundotGameAPI, host: Host): void;

declare class RpcSocialApi implements SocialApi {
    private readonly rpcClient;
    constructor(rpcClient: RpcClient);
    shareLinkAsync(options: {
        shareParams: Record<string, string>;
        metadata?: ShareMetadata;
        slug?: string;
    }): Promise<ShareLinkResult>;
    createQRCodeAsync(options: {
        shareParams: Record<string, string>;
        metadata?: ShareMetadata;
        qrOptions?: SocialQRCodeOptions;
        slug?: string;
    }): Promise<QRCodeResult>;
    addShareClickDataAsync(options: {
        shareLinkId: string;
        metadata: Record<string, string>;
    }): Promise<void>;
    getShareClicksAsync(options: {
        shareLinkId: string;
    }): Promise<{
        clicks: ShareClickData[];
        truncated: boolean;
    }>;
    getMyShareClickDataAsync(options: {
        shareLinkId: string;
    }): Promise<ShareClickData | null>;
    shareFileAsync(options: ShareFileOptions): Promise<ShareFileResult>;
    canShareFileAsync(): Promise<CanShareFileResult>;
    composeSocialPostAsync(options: ComposeSocialPostOptions): Promise<ComposeSocialPostResult>;
    openXFollowMeLinkAsync(): Promise<FollowMeResult>;
    openInstagramFollowMeLinkAsync(): Promise<FollowMeResult>;
    openTikTokFollowMeLinkAsync(): Promise<FollowMeResult>;
    openDiscordFollowMeLinkAsync(): Promise<FollowMeResult>;
    getConfiguredFollowMePlatformsAsync(): Promise<FollowMePlatform[]>;
    private openFollowMeLink;
    private toBase64;
}

declare class MockSocialApi implements SocialApi {
    shareLinkAsync(options: {
        shareParams: Record<string, string>;
        metadata?: ShareMetadata;
        slug?: string;
    }): Promise<ShareLinkResult>;
    createQRCodeAsync(options: {
        shareParams: Record<string, string>;
        qrOptions?: SocialQRCodeOptions;
        metadata?: ShareMetadata;
        slug?: string;
    }): Promise<QRCodeResult>;
    addShareClickDataAsync(options: {
        shareLinkId: string;
        metadata: Record<string, string>;
    }): Promise<void>;
    getShareClicksAsync(options: {
        shareLinkId: string;
    }): Promise<{
        clicks: ShareClickData[];
        truncated: boolean;
    }>;
    getMyShareClickDataAsync(options: {
        shareLinkId: string;
    }): Promise<ShareClickData | null>;
    shareFileAsync(options: ShareFileOptions): Promise<ShareFileResult>;
    canShareFileAsync(): Promise<CanShareFileResult>;
    composeSocialPostAsync(options: ComposeSocialPostOptions): Promise<ComposeSocialPostResult>;
    openXFollowMeLinkAsync(): Promise<FollowMeResult>;
    openInstagramFollowMeLinkAsync(): Promise<FollowMeResult>;
    openTikTokFollowMeLinkAsync(): Promise<FollowMeResult>;
    openDiscordFollowMeLinkAsync(): Promise<FollowMeResult>;
    getConfiguredFollowMePlatformsAsync(): Promise<FollowMePlatform[]>;
    private createMockUrl;
}

declare function initializeSocial(rundotGameApi: RundotGameAPI, host: Host): void;

declare class RpcEntitlementApi implements EntitlementApi {
    private readonly rpcClient;
    constructor(rpcClient: RpcClient);
    listEntitlements(): Promise<Entitlement[]>;
    getQuantity(entitlementId: string): Promise<number>;
    consumeEntitlement(entitlementId: string, quantity: number, callback?: (entitlement: Entitlement, referenceId: string) => void | Promise<void>, reason?: string, referenceId?: string): Promise<Entitlement>;
    getLedger(entitlementId?: string, limit?: number, startAfter?: number): Promise<LedgerEntry[]>;
}

declare class MockEntitlementApi implements EntitlementApi {
    private entitlements;
    private ledger;
    listEntitlements(): Promise<Entitlement[]>;
    getQuantity(entitlementId: string): Promise<number>;
    consumeEntitlement(entitlementId: string, quantity: number, callback?: (entitlement: Entitlement, referenceId: string) => void | Promise<void>, reason?: string, referenceId?: string): Promise<Entitlement>;
    getLedger(entitlementId?: string, limit?: number, startAfter?: number): Promise<LedgerEntry[]>;
}

declare function initializeEntitlements(rundotGameApiInstance: RundotGameAPI, host: Host): void;

/**
 * Production StatsApi backed by the SDK's RPC bridge.
 *
 * Coalescing model: every `submit()` call within a single synchronous tick
 * joins the same pending batch. The batch is flushed on the next microtask
 * via `Promise.resolve().then(...)` — one RPC per tick. This collapses the
 * common case (game loop calling submit several times in a frame) into a
 * single round-trip without using timers, which the repo style rules
 * disallow for coordination.
 *
 * Per-stat last-write-wins on value; per-stat demux on response — see the
 * StatsApi interface doc for the caller contract.
 *
 * Read methods (`getValue`, `getAllValues`) are direct RPC calls.
 *
 * No lifecycle hooks are wired: a microtask drains at every event-loop
 * checkpoint, so any pending submit is flushed before the browser reaches
 * an unload checkpoint. There is nothing left "pending past tick" to flush.
 */
declare class RpcStatsApi implements StatsApi {
    private readonly rpcClient;
    private pending;
    private scheduledFlush;
    constructor(rpcClient: RpcClient);
    submit(statId: string, value: number): Promise<{
        grants: GrantInfo[];
    }>;
    getValue(statId: string): Promise<number | null>;
    getAllValues(): Promise<Record<string, number>>;
    private scheduleFlush;
    private flushNow;
    private sendBatch;
}

/**
 * In-memory implementation of StatsApi for playground + Storybook environments.
 * No rule eval — `submit()` always resolves with an empty `grants` array.
 * Useful for testing UI flows that read stat values without needing a real
 * backend.
 */
declare class MockStatsApi implements StatsApi {
    private values;
    submit(statId: string, value: number): Promise<{
        grants: GrantInfo[];
    }>;
    getValue(statId: string): Promise<number | null>;
    getAllValues(): Promise<Record<string, number>>;
}

declare function initializeStats(rundotGameApiInstance: RundotGameAPI, host: Host): void;

/**
 * Production CollectiblesApi backed by the SDK's RPC bridge. Both methods are
 * direct RPC calls — no batching. `listCards` is typically called once on
 * game init; `claimVipCard` is a single user-initiated action.
 */
declare class RpcCollectiblesApi implements CollectiblesApi {
    private readonly rpcClient;
    constructor(rpcClient: RpcClient);
    listCards(): Promise<CollectibleCard[]>;
    claimVipCard(seriesId: string, cardId: string): Promise<VipClaimResult>;
}

/** In-memory collectibles API for playground + Storybook. Always returns the
 *  same small fixture catalog; claims always succeed. */
declare class MockCollectiblesApi implements CollectiblesApi {
    private cards;
    listCards(): Promise<CollectibleCard[]>;
    claimVipCard(seriesId: string, cardId: string): Promise<VipClaimResult>;
}

declare function initializeCollectibles(rundotGameApiInstance: RundotGameAPI, host: Host): void;

declare class RpcShopApi implements ShopApi {
    private readonly rpcClient;
    constructor(rpcClient: RpcClient);
    getCatalog(includeInactive?: boolean, includeExpired?: boolean, includeUnreleased?: boolean): Promise<StorefrontResponse>;
    getItemDetail(itemId: string): Promise<StorefrontItem>;
    purchase(itemId: string, idempotencyKey: string): Promise<ShopPurchaseResult>;
    purchaseCollectionItem(collectionId: string, itemId: string, idempotencyKey: string): Promise<ShopPurchaseResult>;
    /**
     * Turn the host's cancel rejection into a resolved result so a dismissed
     * paywall is a RESULT, not an error. Everything else still rejects, so real
     * failures keep reaching the game's catch block.
     *
     * This lives in the SDK rather than the host on purpose: games bundle their
     * own SDK copy, so already-deployed games keep the old rejection until they
     * rebuild, and the new contract holds against every host version that
     * reports the dismissal with `USER_CANCELLED` — old and new alike.
     */
    private callPurchase;
    getOrder(orderId: string): Promise<ShopPurchaseResponse>;
    getOrderHistory(options?: {
        limit?: number;
    }): Promise<ShopOrderHistoryResponse>;
    requestRefund(orderId: string, reasonCode: string): Promise<ShopPurchaseResponse>;
}

declare class MockShopApi implements ShopApi {
    getCatalog(includeInactive?: boolean, includeExpired?: boolean, includeUnreleased?: boolean): Promise<StorefrontResponse>;
    getItemDetail(itemId: string): Promise<StorefrontItem>;
    purchase(itemId: string, _idempotencyKey: string): Promise<ShopPurchaseResponse>;
    purchaseCollectionItem(collectionId: string, itemId: string, _idempotencyKey: string): Promise<ShopPurchaseResponse>;
    getOrder(orderId: string): Promise<ShopPurchaseResponse>;
    getOrderHistory(_options?: {
        limit?: number;
    }): Promise<ShopOrderHistoryResponse>;
    requestRefund(orderId: string, reasonCode: string): Promise<ShopPurchaseResponse>;
}

declare function initializeShop(rundotGameApiInstance: RundotGameAPI, host: Host): void;

/**
 * Canonicalizes a profile into the exact 4-field shape stored on
 * `RundotGameAPI._profileData`. The single source of truth for this shape:
 * INIT_SDK, promptLogin, and the IDENTITY_CHANGED replica handler must all
 * produce byte-identical objects so last-write-wins between them is a no-op.
 */
declare function sanitizeProfile(profile: Profile): Profile;

interface AccessGateConfig {
    isAnonymous: () => boolean;
    promptLogin: () => Promise<PromptLoginResult>;
    isAutoPromptEnabled: () => boolean;
}
declare function createAccessGatedApi<T extends object>(api: T, gatedMethods: (keyof T)[], requiredTier: AccessTier, config: AccessGateConfig): T;

/**
 * Gates a streaming (AsyncIterable-returning) method without breaking the
 * `for await` contract. `createAccessGatedApi` cannot wrap streaming methods
 * because it returns Promises, and a `Promise<AsyncIterable>` is not itself
 * iterable. Instead this returns a fresh AsyncIterable whose iterator runs the
 * access check on first iteration — prompting (and resolving sign-in) BEFORE
 * the underlying stream is started, so the host opens the stream with the
 * now-authenticated token. On denial it throws and never touches the stream.
 */
declare function gateStreaming<TArgs extends unknown[], TChunk>(streamFn: (...args: TArgs) => AsyncIterable<TChunk>, config: AccessGateConfig, requiredTier?: AccessTier): (...args: TArgs) => AsyncIterable<TChunk>;

interface GatableHost {
    accessGate: AccessGateApi;
    textGen: TextGenApi;
    imageGen: ImageGenApi;
    audioGen: AudioGenApi;
    spriteGen: SpriteGenApi;
    threeDGen: ThreeDGenApi;
    ugc: UgcApi;
    rooms: RoomsApi;
    simulation: SimulationApi;
    multiplayer: MultiplayerApi;
}
/**
 * Wraps gated API methods on a host with access-gate checks.
 * Call this once during host construction, after all API instances
 * and the accessGate have been assigned.
 */
declare function applyAccessGates(host: GatableHost): void;

declare class RpcAccessGateApi implements AccessGateApi {
    private readonly rpcClient;
    private readonly rundotGameApi;
    autoPromptLogin: boolean;
    private inFlightLogin;
    constructor(rpcClient: RpcClient, rundotGameApi: RundotGameAPI);
    getAccessTier(): AccessTier;
    isAnonymous(): boolean;
    promptLogin(): Promise<PromptLoginResult>;
    private doPromptLogin;
}

declare class MockAccessGateApi implements AccessGateApi {
    private readonly rundotGameApi;
    autoPromptLogin: boolean;
    constructor(rundotGameApi: RundotGameAPI);
    getAccessTier(): AccessTier;
    isAnonymous(): boolean;
    promptLogin(): Promise<PromptLoginResult>;
}

declare function initializeAccessGate(rundotGameApi: RundotGameAPI, host: Host): void;

declare class RpcVideoApi implements VideoApi {
    private rpcClient;
    constructor(rpcClient: RpcClient);
    requestPiPAsync(input: {
        contentId: string;
        videoUrl: string;
        positionSeconds: number;
        playbackRate?: number;
        contentLabel?: string;
    }): Promise<{
        sessionId: string;
    }>;
    readyForPlaybackResumeAsync(input: {
        sessionId: string;
    }): Promise<void>;
    resumeAckAsync(input: {
        sessionId: string;
    }): Promise<void>;
    onResumeFromNativePlayback(callback: (data: {
        sessionId: string;
        positionSeconds: number;
        contentId: string;
    }) => void): Subscription;
}

declare class MockVideoApi implements VideoApi {
    requestPiPAsync(input: {
        contentId: string;
        videoUrl: string;
        positionSeconds: number;
        playbackRate?: number;
        contentLabel?: string;
    }): Promise<{
        sessionId: string;
    }>;
    readyForPlaybackResumeAsync(input: {
        sessionId: string;
    }): Promise<void>;
    resumeAckAsync(input: {
        sessionId: string;
    }): Promise<void>;
    onResumeFromNativePlayback(callback: (data: {
        sessionId: string;
        positionSeconds: number;
        contentId: string;
    }) => void): Subscription;
}

declare function initializeVideo(rundotGameApi: RundotGameAPI, host: Host): void;

declare class RpcActivityApi implements ActivityApi {
    private readonly rpcClient;
    constructor(rpcClient: RpcClient);
    startActivityAsync(intent: ActivityIntent): Promise<StartActivityResult>;
    updateActivity(input: UpdateActivityInput): void;
    endActivityAsync(input: EndActivityInput): Promise<void>;
    onActivityAction(callback: (event: ActivityActionEvent) => void): Subscription;
}

interface MockActivityApiOptions {
    /** When false, every start reports `skipped/unsupported_surface`. */
    supported?: boolean;
}
declare class MockActivityApi implements ActivityApi {
    private readonly active;
    private readonly listeners;
    private readonly supported;
    constructor(options?: MockActivityApiOptions);
    startActivityAsync(intent: ActivityIntent): Promise<StartActivityResult>;
    updateActivity(input: UpdateActivityInput): void;
    endActivityAsync(input: EndActivityInput): Promise<void>;
    onActivityAction(callback: (event: ActivityActionEvent) => void): Subscription;
    /** Test/playground affordance: pretend a native surface issued an action. */
    simulateActivityAction(event: ActivityActionEvent): void;
}

declare function initializeActivity(rundotGameApi: RundotGameAPI, host: Host): void;

declare enum RundotGameMessageId {
    H5_RESPONSE = "H5_RESPONSE",
    IS_REWARDED_AD_READY = "H5_IS_REWARDED_AD_READY",
    IS_INTERSTITIAL_AD_READY = "H5_IS_INTERSTITIAL_AD_READY",
    SHOW_REWARDED_AD = "H5_SHOW_REWARDED_AD",
    SHOW_INTERSTITIAL_AD = "H5_SHOW_INTERSTITIAL_AD",
    LOG_ANALYTICS_EVENT = "H5_LOG_ANALYTICS_EVENT",
    TRACK_FUNNEL_STEP = "H5_TRACK_FUNNEL_STEP",
    HEARTBEAT = "H5_HEARTBEAT",
    DEVICE_CACHE_GET_ITEM = "H5_DEVICE_CACHE_GET_ITEM",
    DEVICE_CACHE_SET_ITEM = "H5_DEVICE_CACHE_SET_ITEM",
    DEVICE_CACHE_REMOVE_ITEM = "H5_DEVICE_CACHE_REMOVE_ITEM",
    DEVICE_CACHE_CLEAR = "H5_DEVICE_CACHE_CLEAR",
    DEVICE_CACHE_KEY = "H5_DEVICE_CACHE_KEY",
    DEVICE_CACHE_LENGTH = "H5_DEVICE_CACHE_LENGTH",
    APP_STORAGE_GET_ITEM = "H5_APP_STORAGE_GET_ITEM",
    APP_STORAGE_SET_ITEM = "H5_APP_STORAGE_SET_ITEM",
    APP_STORAGE_REMOVE_ITEM = "H5_APP_STORAGE_REMOVE_ITEM",
    APP_STORAGE_CLEAR = "H5_APP_STORAGE_CLEAR",
    APP_STORAGE_KEY = "H5_APP_STORAGE_KEY",
    APP_STORAGE_LENGTH = "H5_APP_STORAGE_LENGTH",
    APP_STORAGE_GET_ALL_ITEMS = "H5_APP_STORAGE_GET_ALL_ITEMS",
    APP_STORAGE_GET_ALL_DATA = "H5_APP_STORAGE_GET_ALL_DATA",
    APP_STORAGE_SET_MULTIPLE_ITEMS = "H5_APP_STORAGE_SET_MULTIPLE_ITEMS",
    APP_STORAGE_REMOVE_MULTIPLE_ITEMS = "H5_APP_STORAGE_REMOVE_MULTIPLE_ITEMS",
    OWNER_STORAGE_GET_ITEM = "H5_OWNER_STORAGE_GET_ITEM",
    OWNER_STORAGE_SET_ITEM = "H5_OWNER_STORAGE_SET_ITEM",
    OWNER_STORAGE_REMOVE_ITEM = "H5_OWNER_STORAGE_REMOVE_ITEM",
    OWNER_STORAGE_CLEAR = "H5_OWNER_STORAGE_CLEAR",
    OWNER_STORAGE_KEY = "H5_OWNER_STORAGE_KEY",
    OWNER_STORAGE_LENGTH = "H5_OWNER_STORAGE_LENGTH",
    OWNER_STORAGE_GET_ALL_ITEMS = "H5_OWNER_STORAGE_GET_ALL_ITEMS",
    OWNER_STORAGE_GET_ALL_DATA = "H5_OWNER_STORAGE_GET_ALL_DATA",
    OWNER_STORAGE_SET_MULTIPLE_ITEMS = "H5_OWNER_STORAGE_SET_MULTIPLE_ITEMS",
    OWNER_STORAGE_REMOVE_MULTIPLE_ITEMS = "H5_OWNER_STORAGE_REMOVE_MULTIPLE_ITEMS",
    GLOBAL_STORAGE_GET_ITEM = "H5_GLOBAL_STORAGE_GET_ITEM",
    GLOBAL_STORAGE_SET_ITEM = "H5_GLOBAL_STORAGE_SET_ITEM",
    GLOBAL_STORAGE_REMOVE_ITEM = "H5_GLOBAL_STORAGE_REMOVE_ITEM",
    GLOBAL_STORAGE_CLEAR = "H5_GLOBAL_STORAGE_CLEAR",
    GLOBAL_STORAGE_KEY = "H5_GLOBAL_STORAGE_KEY",
    GLOBAL_STORAGE_LENGTH = "H5_GLOBAL_STORAGE_LENGTH",
    GLOBAL_STORAGE_GET_ALL_ITEMS = "H5_GLOBAL_STORAGE_GET_ALL_ITEMS",
    GLOBAL_STORAGE_SET_MULTIPLE_ITEMS = "H5_GLOBAL_STORAGE_SET_MULTIPLE_ITEMS",
    GLOBAL_STORAGE_REMOVE_MULTIPLE_ITEMS = "H5_GLOBAL_STORAGE_REMOVE_MULTIPLE_ITEMS",
    SHARED_STORAGE_GET_ITEM = "H5_SHARED_STORAGE_GET_ITEM",
    SHARED_STORAGE_SET_ITEM = "H5_SHARED_STORAGE_SET_ITEM",
    SHARED_STORAGE_REMOVE_ITEM = "H5_SHARED_STORAGE_REMOVE_ITEM",
    SHARED_STORAGE_CLEAR = "H5_SHARED_STORAGE_CLEAR",
    SHARED_STORAGE_KEY = "H5_SHARED_STORAGE_KEY",
    SHARED_STORAGE_LENGTH = "H5_SHARED_STORAGE_LENGTH",
    SHARED_STORAGE_GET_ALL_ITEMS = "H5_SHARED_STORAGE_GET_ALL_ITEMS",
    SHARED_STORAGE_GET_ALL_DATA = "H5_SHARED_STORAGE_GET_ALL_DATA",
    SHARED_STORAGE_SET_MULTIPLE_ITEMS = "H5_SHARED_STORAGE_SET_MULTIPLE_ITEMS",
    SHARED_STORAGE_REMOVE_MULTIPLE_ITEMS = "H5_SHARED_STORAGE_REMOVE_MULTIPLE_ITEMS",
    SHARED_STORAGE_INBOUND_LIST_SOURCES = "H5_SHARED_STORAGE_INBOUND_LIST_SOURCES",
    SHARED_STORAGE_INBOUND_GET = "H5_SHARED_STORAGE_INBOUND_GET",
    SHARED_STORAGE_INBOUND_GET_ALL_FROM_SOURCE = "H5_SHARED_STORAGE_INBOUND_GET_ALL_FROM_SOURCE",
    SHARED_STORAGE_INBOUND_GET_ALL_FOR_KEY = "H5_SHARED_STORAGE_INBOUND_GET_ALL_FOR_KEY",
    AVATAR3D_LOAD = "H5_AVATAR3D_LOAD",
    AVATAR3D_SAVE = "H5_AVATAR3D_SAVE",
    AVATAR3D_DELETE = "H5_AVATAR3D_DELETE",
    H5_STACK_PUSH_REQUEST = "H5_STACK_PUSH_REQUEST",
    H5_STACK_POP_REQUEST = "H5_STACK_POP_REQUEST",
    H5_NAVIGATE_TO_GAME = "H5_NAVIGATE_TO_GAME",
    /** Host pushes refreshed notificationParams when a tap arrives for a running game. */
    NOTIFICATION_PARAMS_UPDATE = "NOTIFICATION_PARAMS_UPDATE",
    /**
     * Fire-and-forget ACK the SDK sends to the host immediately after it
     * registers its `NOTIFICATION_PARAMS_UPDATE` subscription inside
     * `initializeContext`. Lets the host distinguish "SDK booted past READY"
     * from "SDK has a live subscriber" and skip the stash for live taps that
     * are guaranteed to be received.
     */
    NOTIFICATION_PARAMS_SUBSCRIBE_READY = "NOTIFICATION_PARAMS_SUBSCRIBE_READY",
    SCHEDULE_LOCAL_NOTIFICATION = "H5_SCHEDULE_LOCAL_NOTIFICATION",
    CANCEL_LOCAL_NOTIFICATION = "H5_CANCEL_LOCAL_NOTIFICATION",
    GET_ALL_SCHEDULED_LOCAL_NOTIFICATIONS = "H5_GET_ALL_SCHEDULED_LOCAL_NOTIFICATIONS",
    IS_LOCAL_NOTIFICATIONS_ENABLED = "H5_IS_LOCAL_NOTIFICATIONS_ENABLED",
    SET_LOCAL_NOTIFICATIONS_ENABLED = "H5_SET_LOCAL_NOTIFICATIONS_ENABLED",
    MESSAGING_GET_RCS_AVAILABLE = "H5_MESSAGING_GET_RCS_AVAILABLE",
    MESSAGING_SCHEDULE_RCS = "H5_MESSAGING_SCHEDULE_RCS",
    MESSAGING_SUBMIT_MESSAGE = "H5_MESSAGING_SUBMIT_MESSAGE",
    MESSAGING_REQUEST_RCS_OPT_IN = "H5_MESSAGING_REQUEST_RCS_OPT_IN",
    TOAST = "H5_TOAST",
    LIKE_DIALOG = "H5_LIKE_DIALOG",
    LIKE_DIALOG_CAN_SHOW = "H5_LIKE_DIALOG_CAN_SHOW",
    LIKE_STATE = "H5_LIKE_STATE",
    COMMENTS_PANEL = "H5_COMMENTS_PANEL",
    COMMENTS_PANEL_CAN_SHOW = "H5_COMMENTS_PANEL_CAN_SHOW",
    REQUEST_SERVER_TIME = "H5_REQUEST_SERVER_TIME",
    SHARE_LINK = "H5_SHARE_LINK",
    CREATE_SHARE_QRCODE = "H5_CREATE_SHARE_QRCODE",
    SHARE_ADD_CLICK_DATA = "H5_SHARE_ADD_CLICK_DATA",
    SHARE_GET_CLICKS = "H5_SHARE_GET_CLICKS",
    SHARE_GET_MY_CLICK_DATA = "H5_SHARE_GET_MY_CLICK_DATA",
    SHARE_FILE = "H5_SHARE_FILE",
    CAN_SHARE_FILE = "H5_CAN_SHARE_FILE",
    SHARE_COMPOSE_POST = "H5_SHARE_COMPOSE_POST",
    SHARE_OPEN_FOLLOW_ME = "H5_SHARE_OPEN_FOLLOW_ME",
    SHARE_GET_FOLLOW_ME_PLATFORMS = "H5_SHARE_GET_FOLLOW_ME_PLATFORMS",
    GET_RELEASE_NOTES = "H5_GET_RELEASE_NOTES",
    OPEN_RELEASE_NOTES = "H5_OPEN_RELEASE_NOTES",
    AI_CHAT_COMPLETION = "H5_AI_CHAT_COMPLETION",
    AI_GET_AVAILABLE_MODELS = "H5_AI_GET_AVAILABLE_MODELS",
    AI_CHAT_COMPLETION_STREAM_START = "H5_AI_CHAT_COMPLETION_STREAM_START",
    AI_CHAT_COMPLETION_STREAM_ABORT = "H5_AI_CHAT_COMPLETION_STREAM_ABORT",
    AI_CHAT_COMPLETION_STREAM_CHUNK = "H5_AI_CHAT_COMPLETION_STREAM_CHUNK",
    AI_CHAT_COMPLETION_STREAM_DONE = "H5_AI_CHAT_COMPLETION_STREAM_DONE",
    AI_CHAT_COMPLETION_STREAM_ERROR = "H5_AI_CHAT_COMPLETION_STREAM_ERROR",
    TRIGGER_HAPTIC = "H5_TRIGGER_HAPTIC",
    GAMEPAD_SUBSCRIBE = "H5_GAMEPAD_SUBSCRIBE",
    GAMEPAD_UNSUBSCRIBE = "H5_GAMEPAD_UNSUBSCRIBE",
    GAMEPAD_SNAPSHOT = "GAMEPAD_SNAPSHOT",
    GAMEPAD_CONNECTED = "GAMEPAD_CONNECTED",
    GAMEPAD_DISCONNECTED = "GAMEPAD_DISCONNECTED",
    DEBUG = "H5_DEBUG",
    H5_IAP_GET_WALLET = "H5_IAP_GET_WALLET",
    H5_IAP_SPEND_CURRENCY = "H5_IAP_SPEND_CURRENCY",
    H5_IAP_GET_SUBSCRIPTIONS = "H5_IAP_GET_SUBSCRIPTIONS",
    H5_IAP_PURCHASE_SUBSCRIPTION = "H5_IAP_PURCHASE_SUBSCRIPTION",
    H5_IAP_GET_PLAYER_SUBSCRIPTION_INFO = "H5_IAP_GET_PLAYER_SUBSCRIPTION_INFO",
    H5_IAP_HAS_USER_MADE_PURCHASE = "H5_IAP_HAS_USER_MADE_PURCHASE",
    H5_IAP_LIST_DIRECT_PURCHASE_SKUS = "H5_IAP_LIST_DIRECT_PURCHASE_SKUS",
    IAP_SPEND_CURRENCY_COMPLETE = "IAP_SPEND_CURRENCY_COMPLETE",
    IAP_WALLET_UPDATE = "IAP_WALLET_UPDATE",
    H5_CREDITS_GET_BALANCE = "H5_CREDITS_GET_BALANCE",
    H5_CREDITS_GET_SUBSCRIPTION = "H5_CREDITS_GET_SUBSCRIPTION",
    H5_CREDITS_GET_PLANS = "H5_CREDITS_GET_PLANS",
    H5_CREDITS_GET_CONTEXT = "H5_CREDITS_GET_CONTEXT",
    H5_CREDITS_ESTIMATE_GENERATION_COST = "H5_CREDITS_ESTIMATE_GENERATION_COST",
    H5_CREDITS_OPEN_PAYWALL = "H5_CREDITS_OPEN_PAYWALL",
    H5_CREDITS_SET_CONFIG = "H5_CREDITS_SET_CONFIG",
    CREDITS_BALANCE_UPDATE = "CREDITS_BALANCE_UPDATE",
    READY = "READY",
    INIT_SDK = "INITIALIZE_SDK",
    PAUSE = "PAUSE",
    RESUME = "RESUME",
    AWAKE = "AWAKE",
    SLEEP = "SLEEP",
    QUIT = "QUIT",
    IDENTITY_CHANGED = "IDENTITY_CHANGED",
    BACK_BUTTON = "BACK_BUTTON",
    BACK_BUTTON_HANDLER_SET = "H5_BACK_BUTTON_HANDLER_SET",
    /** given the experiment name, returns the entire experiment object as configured for the current user */
    GET_EXPERIMENT = "H5_GET_EXPERIMENT",
    /** returns the boolean value for a feature flag using the statsig parameter store, or the feature flags constants as fallback */
    GET_FEATURE_FLAG = "H5_GET_FEATURE_FLAG",
    /** returns the gate value (boolean) for a given feature gate for the current user */
    GET_FEATURE_GATE = "H5_GET_FEATURE_GATE",
    /** returns the raw liveops client rules + server clock for the game's active tag; the SDK resolves schedule windows client-side */
    GET_LIVEOPS_CONFIG = "H5_GET_LIVEOPS_CONFIG",
    H5_SIMULATION_EXECUTE_RECIPE = "H5_SIMULATION_EXECUTE_RECIPE",
    H5_SIMULATION_GET_ACTIVE_RUNS = "H5_SIMULATION_GET_ACTIVE_RUNS",
    H5_SIMULATION_COLLECT_RECIPE = "H5_SIMULATION_COLLECT_RECIPE",
    H5_SIMULATION_EXECUTE_SCOPED_RECIPE = "H5_SIMULATION_EXECUTE_SCOPED_RECIPE",
    H5_SIMULATION_GET_AVAILABLE_RECIPES = "H5_SIMULATION_GET_AVAILABLE_RECIPES",
    H5_SIMULATION_GET_RECIPE_REQUIREMENTS = "H5_SIMULATION_GET_RECIPE_REQUIREMENTS",
    H5_SIMULATION_GET_BATCH_RECIPE_REQUIREMENTS = "H5_SIMULATION_GET_BATCH_RECIPE_REQUIREMENTS",
    H5_SIMULATION_TRIGGER_RECIPE_CHAIN = "H5_SIMULATION_TRIGGER_RECIPE_CHAIN",
    H5_SIMULATION_RESOLVE_VALUE = "H5_SIMULATION_RESOLVE_VALUE",
    H5_SIMULATION_GET_ENTITY_METADATA = "H5_SIMULATION_GET_ENTITY_METADATA",
    H5_SIMULATION_GET_STATE = "H5_SIMULATION_GET_STATE",
    H5_SIMULATION_GET_CONFIG = "H5_SIMULATION_GET_CONFIG",
    H5_SIMULATION_GET_CONTAINERS = "H5_SIMULATION_GET_CONTAINERS",
    H5_SIMULATION_GET_ASSIGNMENTS = "H5_SIMULATION_GET_ASSIGNMENTS",
    H5_SIMULATION_ASSIGN_ITEM = "H5_SIMULATION_ASSIGN_ITEM",
    H5_SIMULATION_REMOVE_ITEM = "H5_SIMULATION_REMOVE_ITEM",
    H5_SIMULATION_CALCULATE_POWER_PREVIEW = "H5_SIMULATION_CALCULATE_POWER_PREVIEW",
    H5_SIMULATION_GET_AVAILABLE_ITEMS = "H5_SIMULATION_GET_AVAILABLE_ITEMS",
    H5_SIMULATION_VALIDATE_ASSIGNMENT = "H5_SIMULATION_VALIDATE_ASSIGNMENT",
    H5_SIMULATION_BATCH_OPERATIONS = "H5_SIMULATION_BATCH_OPERATIONS",
    H5_SIMULATION_SUBSCRIBE = "H5_SIMULATION_SUBSCRIBE",
    H5_SIMULATION_UNSUBSCRIBE = "H5_SIMULATION_UNSUBSCRIBE",
    H5_SIMULATION_UPDATE = "H5_SIMULATION_UPDATE",
    H5_SIMULATION_RESET_STATE = "H5_SIMULATION_RESET_STATE",
    H5_LEADERBOARD_CREATE_SCORE_TOKEN = "H5_LEADERBOARD_CREATE_SCORE_TOKEN",
    H5_LEADERBOARD_SUBMIT_SCORE = "H5_LEADERBOARD_SUBMIT_SCORE",
    H5_LEADERBOARD_GET_PAGED_SCORES = "H5_LEADERBOARD_GET_PAGED_SCORES",
    H5_LEADERBOARD_GET_PODIUM_SCORES = "H5_LEADERBOARD_GET_PODIUM_SCORES",
    H5_LEADERBOARD_GET_MY_RANK = "H5_LEADERBOARD_GET_MY_RANK",
    H5_ROOM_CREATE = "H5_ROOM_CREATE",
    H5_ROOM_JOIN = "H5_ROOM_JOIN",
    H5_ROOM_JOIN_OR_CREATE = "H5_ROOM_JOIN_OR_CREATE",
    H5_ROOM_LEAVE = "H5_ROOM_LEAVE",
    H5_ROOM_UPDATE_DATA = "H5_ROOM_UPDATE_DATA",
    H5_ROOM_GET_DATA = "H5_ROOM_GET_DATA",
    H5_ROOM_SUBSCRIBE = "H5_ROOM_SUBSCRIBE",
    H5_ROOM_UNSUBSCRIBE = "H5_ROOM_UNSUBSCRIBE",
    H5_ROOM_SEND_MESSAGE = "H5_ROOM_SEND_MESSAGE",
    H5_ROOM_GET_MESSAGES = "H5_ROOM_GET_MESSAGES",
    H5_ROOM_LIST_ROOMS = "H5_ROOM_LIST_ROOMS",
    H5_ROOM_LIST_PUBLIC = "H5_ROOM_LIST_PUBLIC",
    H5_ROOM_SEARCH = "H5_ROOM_SEARCH",
    H5_ROOM_JOIN_BY_CODE = "H5_ROOM_JOIN_BY_CODE",
    H5_ROOM_GET_USER_ROOMS = "H5_ROOM_GET_USER_ROOMS",
    H5_ROOM_GET_PLAYERS = "H5_ROOM_GET_PLAYERS",
    H5_ROOM_UPDATE_PLAYER_DATA = "H5_ROOM_UPDATE_PLAYER_DATA",
    H5_ROOM_START_GAME = "H5_ROOM_START_GAME",
    H5_ROOM_PROPOSE_MOVE = "h5:room:proposeMove",
    H5_ROOM_END_GAME = "H5_ROOM_END_GAME",
    H5_ROOM_KICK_PLAYER = "H5_ROOM_KICK_PLAYER",
    H5_ROOM_PROMOTE_TO_SPECTATOR = "H5_ROOM_PROMOTE_TO_SPECTATOR",
    H5_LOAD_EMBEDDED_ASSET = "H5_LOAD_EMBEDDED_ASSET",
    H5_LOAD_EMBEDDED_ASSET_BEGIN = "H5_LOAD_EMBEDDED_ASSET_BEGIN",
    H5_LOAD_EMBEDDED_ASSET_CHUNK = "H5_LOAD_EMBEDDED_ASSET_CHUNK",
    H5_SHOW_LOAD_SCREEN = "H5_SHOW_LOAD_SCREEN",
    H5_HIDE_LOAD_SCREEN = "H5_HIDE_LOAD_SCREEN",
    H5_SET_LOADER_TEXT = "H5_SET_LOADER_TEXT",
    H5_SET_LOADER_PROGRESS = "H5_SET_LOADER_PROGRESS",
    H5_IAP_OPEN_STORE = "H5_IAP_OPEN_STORE",
    H5_IAP_GET_CURRENCY_ICON = "H5_IAP_GET_CURRENCY_ICON",
    H5_UGC_CREATE = "H5_UGC_CREATE",
    H5_UGC_UPDATE = "H5_UGC_UPDATE",
    H5_UGC_DELETE = "H5_UGC_DELETE",
    H5_UGC_GET = "H5_UGC_GET",
    H5_UGC_LIST_MINE = "H5_UGC_LIST_MINE",
    H5_UGC_LIST_SHARED = "H5_UGC_LIST_SHARED",
    H5_UGC_LIST_MEMBERS = "H5_UGC_LIST_MEMBERS",
    H5_UGC_ADD_MEMBER = "H5_UGC_ADD_MEMBER",
    H5_UGC_REMOVE_MEMBER = "H5_UGC_REMOVE_MEMBER",
    H5_UGC_BROWSE = "H5_UGC_BROWSE",
    H5_UGC_LIKE = "H5_UGC_LIKE",
    H5_UGC_UNLIKE = "H5_UGC_UNLIKE",
    H5_UGC_RECORD_USE = "H5_UGC_RECORD_USE",
    H5_UGC_REPORT = "H5_UGC_REPORT",
    H5_UGC_GET_MANY = "H5_UGC_GET_MANY",
    H5_UGC_COUNT = "H5_UGC_COUNT",
    H5_UGC_FOLLOW = "H5_UGC_FOLLOW",
    H5_UGC_UNFOLLOW = "H5_UGC_UNFOLLOW",
    H5_UGC_IS_FOLLOWING = "H5_UGC_IS_FOLLOWING",
    H5_UGC_GET_FOLLOW_COUNTS = "H5_UGC_GET_FOLLOW_COUNTS",
    H5_UGC_VOTING_VOTE = "H5_UGC_VOTING_VOTE",
    H5_UGC_VOTING_UNVOTE = "H5_UGC_VOTING_UNVOTE",
    H5_UGC_VOTING_GET_MY_VOTES = "H5_UGC_VOTING_GET_MY_VOTES",
    H5_UGC_VOTING_GET_LEADERBOARD = "H5_UGC_VOTING_GET_LEADERBOARD",
    H5_UGC_VOTING_GET_WINNERS = "H5_UGC_VOTING_GET_WINNERS",
    H5_UGC_CROSS_APP_BROWSE = "H5_UGC_CROSS_APP_BROWSE",
    H5_UGC_CROSS_APP_GET = "H5_UGC_CROSS_APP_GET",
    H5_UGC_CROSS_APP_GET_MANY = "H5_UGC_CROSS_APP_GET_MANY",
    H5_UGC_CROSS_APP_COUNT = "H5_UGC_CROSS_APP_COUNT",
    H5_UGC_CROSS_APP_CREATE = "H5_UGC_CROSS_APP_CREATE",
    H5_UGC_CROSS_APP_UPDATE = "H5_UGC_CROSS_APP_UPDATE",
    H5_UGC_CROSS_APP_DELETE = "H5_UGC_CROSS_APP_DELETE",
    H5_UGC_GET_USER_RECOMMENDATION = "H5_UGC_GET_USER_RECOMMENDATION",
    H5_UGC_GET_ITEM_RECOMMENDATION = "H5_UGC_GET_ITEM_RECOMMENDATION",
    H5_UGC_GET_COMPOSITE_RECOMMENDATION = "H5_UGC_GET_COMPOSITE_RECOMMENDATION",
    H5_UGC_GET_MORE_RECOMMENDATIONS = "H5_UGC_GET_MORE_RECOMMENDATIONS",
    H5_UGC_ADD_DETAIL_VIEW = "H5_UGC_ADD_DETAIL_VIEW",
    H5_UGC_ADD_RATING = "H5_UGC_ADD_RATING",
    H5_UGC_ADD_CART_ADDITION = "H5_UGC_ADD_CART_ADDITION",
    H5_UGC_SET_VIEW_PORTION = "H5_UGC_SET_VIEW_PORTION",
    CLIPS_GET_CAPTURE_CONSENT = "H5_CLIPS_GET_CAPTURE_CONSENT",
    CLIPS_REQUEST_CAPTURE_CONSENT = "H5_CLIPS_REQUEST_CAPTURE_CONSENT",
    CLIPS_SET_CAPTURE_CONSENT = "H5_CLIPS_SET_CAPTURE_CONSENT",
    H5_IMAGE_GEN_GENERATE = "H5_IMAGE_GEN_GENERATE",
    H5_IMAGE_GEN_ESTIMATE_DEPTH = "H5_IMAGE_GEN_ESTIMATE_DEPTH",
    H5_IMAGE_GEN_REMOVE_BACKGROUND = "H5_IMAGE_GEN_REMOVE_BACKGROUND",
    H5_IMAGE_GEN_UPSCALE_IMAGE = "H5_IMAGE_GEN_UPSCALE_IMAGE",
    H5_AUDIO_GEN_GENERATE = "H5_AUDIO_GEN_GENERATE",
    H5_AUDIO_GEN_LIST_VOICES = "H5_AUDIO_GEN_LIST_VOICES",
    H5_AUDIO_GEN_DESIGN_VOICES = "H5_AUDIO_GEN_DESIGN_VOICES",
    H5_AUDIO_GEN_SAVE_DESIGNED_VOICE = "H5_AUDIO_GEN_SAVE_DESIGNED_VOICE",
    H5_VIDEO_GEN_GENERATE = "H5_VIDEO_GEN_GENERATE",
    H5_VIDEO_GEN_CANCEL = "H5_VIDEO_GEN_CANCEL",
    H5_VIDEO_GEN_JOB_STARTED = "H5_VIDEO_GEN_JOB_STARTED",
    H5_FILES_UPLOAD = "H5_FILES_UPLOAD",
    H5_FILES_BATCH_UPLOAD = "H5_FILES_BATCH_UPLOAD",
    H5_FILES_BATCH_COPY = "H5_FILES_BATCH_COPY",
    H5_FILES_CONFIRM_UPLOAD = "H5_FILES_CONFIRM_UPLOAD",
    H5_FILES_BATCH_CONFIRM = "H5_FILES_BATCH_CONFIRM",
    H5_FILES_GET_URL = "H5_FILES_GET_URL",
    H5_FILES_GET_URLS = "H5_FILES_GET_URLS",
    H5_FILES_GET_METADATA = "H5_FILES_GET_METADATA",
    H5_FILES_EXISTS = "H5_FILES_EXISTS",
    H5_FILES_BATCH_METADATA = "H5_FILES_BATCH_METADATA",
    H5_FILES_BATCH_EXISTS = "H5_FILES_BATCH_EXISTS",
    H5_FILES_DELETE = "H5_FILES_DELETE",
    H5_FILES_LIST = "H5_FILES_LIST",
    H5_FILES_GET_QUOTA = "H5_FILES_GET_QUOTA",
    H5_FILES_SET_VISIBILITY = "H5_FILES_SET_VISIBILITY",
    H5_FILES_TRANSFORM = "H5_FILES_TRANSFORM",
    H5_FILES_EXPORT_CLOUDINARY = "H5_FILES_EXPORT_CLOUDINARY",
    H5_SPRITE_GEN_GENERATE = "H5_SPRITE_GEN_GENERATE",
    H5_SPRITE_GEN_ANIMATE = "H5_SPRITE_GEN_ANIMATE",
    H5_SPRITE_GEN_CHARACTER_ANIMATE = "H5_SPRITE_GEN_CHARACTER_ANIMATE",
    H5_SPRITE_GEN_LIST_CHARACTER_WORKFLOWS = "H5_SPRITE_GEN_LIST_CHARACTER_WORKFLOWS",
    H5_SPRITE_GEN_LIST_MODELS = "H5_SPRITE_GEN_LIST_MODELS",
    H5_SPRITE_GEN_GET_COSTS = "H5_SPRITE_GEN_GET_COSTS",
    H5_THREE_D_GEN_GENERATE = "H5_THREE_D_GEN_GENERATE",
    H5_THREE_D_GEN_REMESH = "H5_THREE_D_GEN_REMESH",
    H5_THREE_D_GEN_RIG = "H5_THREE_D_GEN_RIG",
    H5_THREE_D_GEN_ANIMATE = "H5_THREE_D_GEN_ANIMATE",
    H5_PROMPT_LOGIN = "H5_PROMPT_LOGIN",
    H5_ENTITLEMENT_LIST = "H5_ENTITLEMENT_LIST",
    H5_ENTITLEMENT_CONSUME = "H5_ENTITLEMENT_CONSUME",
    H5_ENTITLEMENT_GET_QUANTITY = "H5_ENTITLEMENT_GET_QUANTITY",
    H5_ENTITLEMENT_GET_LEDGER = "H5_ENTITLEMENT_GET_LEDGER",
    H5_CHOICES_GET_BALANCE = "H5_CHOICES_GET_BALANCE",
    H5_CHOICES_SPEND = "H5_CHOICES_SPEND",
    H5_STATS_SUBMIT_BATCH = "H5_STATS_SUBMIT_BATCH",
    H5_STATS_GET_VALUE = "H5_STATS_GET_VALUE",
    H5_STATS_GET_ALL = "H5_STATS_GET_ALL",
    H5_COLLECTIBLES_LIST_CARDS = "H5_COLLECTIBLES_LIST_CARDS",
    H5_COLLECTIBLES_VIP_CLAIM = "H5_COLLECTIBLES_VIP_CLAIM",
    H5_SHOP_GET_CATALOG = "H5_SHOP_GET_CATALOG",
    H5_SHOP_GET_ITEM_DETAIL = "H5_SHOP_GET_ITEM_DETAIL",
    H5_SHOP_PURCHASE = "H5_SHOP_PURCHASE",
    H5_SHOP_GET_ORDER_STATUS = "H5_SHOP_GET_ORDER_STATUS",
    H5_SHOP_GET_ORDER_HISTORY = "H5_SHOP_GET_ORDER_HISTORY",
    H5_SHOP_REQUEST_REFUND = "H5_SHOP_REQUEST_REFUND",
    H5_CDN_RESOLVE_ASSET_URL = "H5_CDN_RESOLVE_ASSET_URL",
    H5_CDN_RESOLVE_ASSET_URLS = "H5_CDN_RESOLVE_ASSET_URLS",
    H5_CDN_REFRESH_ENTITLEMENTS = "H5_CDN_REFRESH_ENTITLEMENTS",
    H5_APP_GET_MY_ROLE = "H5_APP_GET_MY_ROLE",
    H5_GET_ATTRIBUTION_PARAMS = "H5_GET_ATTRIBUTION_PARAMS",
    H5_RESOLVE_LAUNCH_INTENT = "H5_RESOLVE_LAUNCH_INTENT",
    H5_APP_ADMIN_UGC_BROWSE = "H5_APP_ADMIN_UGC_BROWSE",
    H5_APP_ADMIN_UGC_REMOVE_ENTRY = "H5_APP_ADMIN_UGC_REMOVE_ENTRY",
    H5_APP_ADMIN_UGC_LIST_REPORTS = "H5_APP_ADMIN_UGC_LIST_REPORTS",
    H5_APP_ADMIN_UGC_RESOLVE_REPORT = "H5_APP_ADMIN_UGC_RESOLVE_REPORT",
    H5_APP_ADMIN_IMAGEGEN_BROWSE = "H5_APP_ADMIN_IMAGEGEN_BROWSE",
    H5_APP_ADMIN_IMAGEGEN_REMOVE_ENTRY = "H5_APP_ADMIN_IMAGEGEN_REMOVE_ENTRY",
    H5_APP_ADMIN_IMAGEGEN_LIST_REPORTS = "H5_APP_ADMIN_IMAGEGEN_LIST_REPORTS",
    H5_APP_ADMIN_IMAGEGEN_RESOLVE_REPORT = "H5_APP_ADMIN_IMAGEGEN_RESOLVE_REPORT",
    H5_APP_ADMIN_VIDEOGEN_BROWSE = "H5_APP_ADMIN_VIDEOGEN_BROWSE",
    H5_APP_ADMIN_VIDEOGEN_REMOVE_ENTRY = "H5_APP_ADMIN_VIDEOGEN_REMOVE_ENTRY",
    H5_APP_ADMIN_VIDEOGEN_LIST_REPORTS = "H5_APP_ADMIN_VIDEOGEN_LIST_REPORTS",
    H5_APP_ADMIN_VIDEOGEN_RESOLVE_REPORT = "H5_APP_ADMIN_VIDEOGEN_RESOLVE_REPORT",
    H5_APP_ADMIN_AUDIOGEN_BROWSE = "H5_APP_ADMIN_AUDIOGEN_BROWSE",
    H5_APP_ADMIN_AUDIOGEN_REMOVE_ENTRY = "H5_APP_ADMIN_AUDIOGEN_REMOVE_ENTRY",
    H5_APP_ADMIN_AUDIOGEN_LIST_REPORTS = "H5_APP_ADMIN_AUDIOGEN_LIST_REPORTS",
    H5_APP_ADMIN_AUDIOGEN_RESOLVE_REPORT = "H5_APP_ADMIN_AUDIOGEN_RESOLVE_REPORT",
    H5_APP_ADMIN_SPRITEGEN_BROWSE = "H5_APP_ADMIN_SPRITEGEN_BROWSE",
    H5_APP_ADMIN_SPRITEGEN_REMOVE_ENTRY = "H5_APP_ADMIN_SPRITEGEN_REMOVE_ENTRY",
    H5_APP_ADMIN_SPRITEGEN_LIST_REPORTS = "H5_APP_ADMIN_SPRITEGEN_LIST_REPORTS",
    H5_APP_ADMIN_SPRITEGEN_RESOLVE_REPORT = "H5_APP_ADMIN_SPRITEGEN_RESOLVE_REPORT",
    H5_APP_ADMIN_THREEDGEN_BROWSE = "H5_APP_ADMIN_THREEDGEN_BROWSE",
    H5_APP_ADMIN_THREEDGEN_REMOVE_ENTRY = "H5_APP_ADMIN_THREEDGEN_REMOVE_ENTRY",
    H5_APP_ADMIN_THREEDGEN_LIST_REPORTS = "H5_APP_ADMIN_THREEDGEN_LIST_REPORTS",
    H5_APP_ADMIN_THREEDGEN_RESOLVE_REPORT = "H5_APP_ADMIN_THREEDGEN_RESOLVE_REPORT",
    H5_REQUEST_JOIN_TICKET = "H5_REQUEST_JOIN_TICKET",
    H5_LIST_USER_REALTIME_ROOMS = "H5_LIST_USER_REALTIME_ROOMS",
    H5_VIDEO_REQUEST_PIP = "H5_VIDEO_REQUEST_PIP",
    H5_VIDEO_WEB_READY = "H5_VIDEO_WEB_READY",
    H5_VIDEO_RESUME_ACK = "H5_VIDEO_RESUME_ACK",
    H5_VIDEO_RESUME_FROM_NATIVE_PLAYBACK = "H5_VIDEO_RESUME_FROM_NATIVE_PLAYBACK",
    H5_SYSTEM_CAN_ADD_TO_HOME_SCREEN = "H5_SYSTEM_CAN_ADD_TO_HOME_SCREEN",
    H5_SYSTEM_ADD_TO_HOME_SCREEN = "H5_SYSTEM_ADD_TO_HOME_SCREEN",
    H5_POLL_COMPLETED_JOBS = "H5_POLL_COMPLETED_JOBS",
    ACTIVITY_START = "H5_ACTIVITY_START",
    ACTIVITY_UPDATE = "H5_ACTIVITY_UPDATE",
    ACTIVITY_END = "H5_ACTIVITY_END",
    ACTIVITY_ACTION = "ACTIVITY_ACTION"
}

declare class RpcAppApi implements AppApi {
    private readonly rpcClient;
    readonly adminUgc: AdminUgcApi;
    readonly adminImageGen: AdminImageGenApi;
    readonly adminVideoGen: AdminVideoGenApi;
    readonly adminSpriteGen: AdminSpriteGenApi;
    readonly adminAudioGen: AdminAudioGenApi;
    readonly adminThreeDGen: AdminThreeDGenApi;
    constructor(rpcClient: RpcClient);
    getMyRole(): Promise<AppRole>;
    resolveLaunchIntent(options?: ResolveLaunchIntentOptions): Promise<LaunchIntent>;
    getReleaseNotesAsync(): Promise<ReleaseNote[]>;
    openReleaseNotesAsync(): Promise<void>;
}

declare class MockAppApi implements AppApi {
    private readonly rundotGameApi;
    readonly adminUgc: AdminUgcApi;
    readonly adminImageGen: AdminImageGenApi;
    readonly adminVideoGen: AdminVideoGenApi;
    readonly adminSpriteGen: AdminSpriteGenApi;
    readonly adminAudioGen: AdminAudioGenApi;
    readonly adminThreeDGen: AdminThreeDGenApi;
    constructor(rundotGameApi: RundotGameAPI);
    getMyRole(): Promise<AppRole>;
    resolveLaunchIntent(_options?: ResolveLaunchIntentOptions): Promise<LaunchIntent>;
    getReleaseNotesAsync(): Promise<ReleaseNote[]>;
    openReleaseNotesAsync(): Promise<void>;
}

declare class RpcAdminUgcApi implements AdminUgcApi {
    private readonly rpcClient;
    constructor(rpcClient: RpcClient);
    browse(params?: AdminUgcBrowseParams): Promise<AdminUgcBrowseResponse>;
    removeEntry(entryId: string): Promise<void>;
    listReports(params?: AdminUgcListReportsParams): Promise<AdminUgcListReportsResponse>;
    resolveReport(reportId: string, action: AdminUgcResolveAction): Promise<void>;
}

declare class MockAdminUgcApi implements AdminUgcApi {
    browse(_params?: AdminUgcBrowseParams): Promise<AdminUgcBrowseResponse>;
    removeEntry(_entryId: string): Promise<void>;
    listReports(_params?: AdminUgcListReportsParams): Promise<AdminUgcListReportsResponse>;
    resolveReport(_reportId: string, _action: AdminUgcResolveAction): Promise<void>;
}

declare class RpcAdminImageGenApi implements AdminImageGenApi {
    private readonly delegate;
    constructor(rpcClient: RpcClient);
    browse(params?: AdminGenBrowseParams): Promise<AdminGenBrowseResponse<ImageGenEntry>>;
    removeEntry(generationId: string): Promise<void>;
    listReports(params?: AdminGenListReportsParams): Promise<AdminGenListReportsResponse<ImageGenReport>>;
    resolveReport(reportId: string, action: AdminGenResolveAction): Promise<void>;
}
declare class RpcAdminSpriteGenApi implements AdminSpriteGenApi {
    private readonly delegate;
    constructor(rpcClient: RpcClient);
    browse(params?: AdminGenBrowseParams): Promise<AdminGenBrowseResponse<SpriteGenEntry>>;
    removeEntry(generationId: string): Promise<void>;
    listReports(params?: AdminGenListReportsParams): Promise<AdminGenListReportsResponse<SpriteGenReport>>;
    resolveReport(reportId: string, action: AdminGenResolveAction): Promise<void>;
}
declare class RpcAdminThreeDGenApi implements AdminThreeDGenApi {
    private readonly delegate;
    constructor(rpcClient: RpcClient);
    browse(params?: AdminGenBrowseParams): Promise<AdminGenBrowseResponse<ThreeDGenEntry>>;
    removeEntry(generationId: string): Promise<void>;
    listReports(params?: AdminGenListReportsParams): Promise<AdminGenListReportsResponse<ThreeDGenReport>>;
    resolveReport(reportId: string, action: AdminGenResolveAction): Promise<void>;
}
declare class MockAdminImageGenApi implements AdminImageGenApi {
    private readonly delegate;
    browse(params?: AdminGenBrowseParams): Promise<AdminGenBrowseResponse<ImageGenEntry>>;
    removeEntry(generationId: string): Promise<void>;
    listReports(params?: AdminGenListReportsParams): Promise<AdminGenListReportsResponse<ImageGenReport>>;
    resolveReport(reportId: string, action: AdminGenResolveAction): Promise<void>;
}
declare class MockAdminSpriteGenApi implements AdminSpriteGenApi {
    private readonly delegate;
    browse(params?: AdminGenBrowseParams): Promise<AdminGenBrowseResponse<SpriteGenEntry>>;
    removeEntry(generationId: string): Promise<void>;
    listReports(params?: AdminGenListReportsParams): Promise<AdminGenListReportsResponse<SpriteGenReport>>;
    resolveReport(reportId: string, action: AdminGenResolveAction): Promise<void>;
}
declare class MockAdminThreeDGenApi implements AdminThreeDGenApi {
    private readonly delegate;
    browse(params?: AdminGenBrowseParams): Promise<AdminGenBrowseResponse<ThreeDGenEntry>>;
    removeEntry(generationId: string): Promise<void>;
    listReports(params?: AdminGenListReportsParams): Promise<AdminGenListReportsResponse<ThreeDGenReport>>;
    resolveReport(reportId: string, action: AdminGenResolveAction): Promise<void>;
}

declare function initializeApp(rundotGameApiInstance: RundotGameAPI, host: Host): void;

declare class RpcAvatarApi implements Avatar3dApi {
    private readonly rundotGameApi;
    private readonly rpcClient;
    constructor(rpcClient: RpcClient, rundotGameApi: RundotGameAPI);
    downloadAssetPaths(): Promise<Record<string, string[]>>;
    downloadManifest(): Promise<AssetManifest>;
    loadAvatar(avatarId?: string): Promise<Avatar3dConfig | null>;
    saveAvatar(config: Avatar3dConfig): Promise<string>;
    deleteAvatar(): Promise<void>;
    showEditor(options?: ShowEditorOptions): Promise<Avatar3dEdits>;
    private fetchFromCdn;
}

declare function initializeAvatar3d(rundotGameApi: RundotGameAPI, host: Host): void;

declare class RpcAdsApi implements AdsApi {
    private readonly rpcClient;
    constructor(rpcClient: RpcClient);
    showInterstitialAd(options?: ShowInterstitialAdOptions): Promise<boolean>;
    isInterstitialAdReadyAsync(): Promise<boolean>;
    isRewardedAdReadyAsync(): Promise<boolean>;
    showRewardedAdAsync(options?: ShowRewardedAdOptions): Promise<boolean>;
}

declare class MockAdsApi implements AdsApi {
    private mockOverlay;
    constructor(mockOverlay: MockOverlay);
    isRewardedAdReadyAsync(): Promise<boolean>;
    isInterstitialAdReadyAsync(): Promise<boolean>;
    showRewardedAdAsync(options?: ShowRewardedAdOptions): Promise<boolean>;
    showInterstitialAd(options?: ShowInterstitialAdOptions): Promise<boolean>;
}

declare function initializeAds(rundotGameApiInstance: RundotGameAPI, host: Host): void;

declare function initializeTextGen(rundotGameApi: RundotGameAPI, host: Host): void;

declare class RpcImageGenApi implements ImageGenApi {
    private readonly rpcClient;
    constructor(rpcClient: RpcClient);
    generate(params: ImageGenParams): Promise<ImageGenResult>;
    estimateDepth(params: DepthEstimationParams): Promise<DepthEstimationResult>;
    removeBackground(params: RemoveBackgroundParams): Promise<RemoveBackgroundResult>;
    upscaleImage(params: UpscaleImageParams): Promise<UpscaleImageResult>;
    getCompletedJobs(): Promise<ImageGenJobEvent[]>;
}

declare class MockImageGenApi implements ImageGenApi {
    generate(_params: ImageGenParams): Promise<ImageGenResult>;
    estimateDepth(_params: DepthEstimationParams): Promise<DepthEstimationResult>;
    removeBackground(_params: RemoveBackgroundParams): Promise<RemoveBackgroundResult>;
    upscaleImage(_params: UpscaleImageParams): Promise<UpscaleImageResult>;
    getCompletedJobs(): Promise<ImageGenJobEvent[]>;
}

declare function initializeImageGen(rundotGameApi: RundotGameAPI, host: Host): void;

declare class RpcSpriteGenApi implements SpriteGenApi {
    private readonly rpcClient;
    constructor(rpcClient: RpcClient);
    generate(params: SpriteGenParams): Promise<SpriteGenResult>;
    animate(params: AnimateSpriteParams): Promise<AnimateSpriteResult>;
    characterAnimate(params: CharacterAnimateParams): Promise<{
        jobId: string;
    }>;
    listCharacterWorkflows(): Promise<CharacterWorkflow[]>;
    listModels(): Promise<unknown>;
    getCosts(): Promise<SpriteGenCosts>;
    getCompletedJobs(): Promise<SpriteGenJobEvent[]>;
}

declare class MockSpriteGenApi implements SpriteGenApi {
    generate(_params: SpriteGenParams): Promise<SpriteGenResult>;
    animate(_params: AnimateSpriteParams): Promise<AnimateSpriteResult>;
    characterAnimate(_params: CharacterAnimateParams): Promise<{
        jobId: string;
    }>;
    listCharacterWorkflows(): Promise<CharacterWorkflow[]>;
    listModels(): Promise<unknown>;
    getCosts(): Promise<SpriteGenCosts>;
    getCompletedJobs(): Promise<SpriteGenJobEvent[]>;
}

declare function initializeSpriteGen(rundotGameApi: RundotGameAPI, host: Host): void;

declare const SDK_VERSION: string;

/**
 * Shared debug flag for all mock API implementations.
 * Enable to trace mock API calls during local development:
 *
 *   import { MockDebug } from '@series-inc/rundot-game-sdk'
 *   MockDebug.enabled = true
 */
declare const MockDebug: {
    enabled: boolean;
};
declare function mockLog(tag: string, message: string, ...args: unknown[]): void;

declare class RemoteHost implements Host {
    readonly ads: AdsApi;
    readonly analytics: AnalyticsApi;
    readonly deviceCache: StorageApi;
    readonly appStorage: StorageApi;
    readonly ownerStorage: StorageApi;
    readonly sharedStorage: SharedStorageHostApi;
    readonly avatar3d: Avatar3dApi;
    readonly navigation: NavigationApi;
    readonly notifications: NotificationsApi;
    readonly popups: PopupsApi;
    readonly profile: ProfileApi;
    readonly system: SystemApi;
    readonly cdn: CdnApi;
    readonly time: TimeApi;
    ai: AiApi;
    textGen: TextGenApi;
    readonly haptics: HapticsApi;
    readonly gamepad: GamepadApi;
    readonly features: FeaturesApi;
    readonly liveops: LiveOpsApi;
    readonly lifecycle: LifecycleApi;
    simulation: SimulationApi;
    rooms: RoomsApi;
    readonly logging: LoggingApi;
    readonly iap: IapApi;
    readonly credits: CreditsApi;
    readonly leaderboard: LeaderboardApi;
    ugc: UgcApi;
    readonly preloader: PreloaderApi;
    readonly social: SocialApi;
    imageGen: ImageGenApi;
    audioGen: AudioGenApi;
    videoGen: VideoGenApi;
    files: FilesApi;
    clips: ClipsApi;
    spriteGen: SpriteGenApi;
    threeDGen: ThreeDGenApi;
    readonly entitlements: EntitlementApi;
    readonly choices: ChoicesApi;
    readonly stats: StatsApi;
    readonly collectibles: CollectiblesApi;
    readonly shop: ShopApi;
    readonly accessGate: AccessGateApi;
    readonly multiplayer: MultiplayerApi;
    readonly video: VideoApi;
    readonly activity: ActivityApi;
    readonly app: AppApi;
    readonly attribution: AttributionApi;
    context?: InitializationContext;
    instanceId: string;
    get isInitialized(): boolean;
    private readonly rundotGameApi;
    private readonly rpcClient;
    private _isInitialized;
    private _roomServerUrl;
    constructor(rundotGameApi: RundotGameAPI);
    initialize(options?: InitializationOptions): Promise<InitializationContext>;
    onNotificationParamsUpdate(callback: (params: Record<string, string>) => void): () => void;
    notifyNotificationParamsSubscribeReady(): void;
}
/** Snapshot of the INIT_SDK intent fields used to build the launch context. */
interface InitializationContextSnapshot {
    safeArea?: SafeArea;
    initializeAsleep: boolean;
    launchParams?: Record<string, string>;
    shareParams?: Record<string, string>;
    notificationParams?: Record<string, string>;
    shareLinkId?: string;
    g2gLaunch?: Record<string, unknown>;
    g2gReturn?: Record<string, unknown>;
}
/** Invoked once per intent field per session, the first time it is read. */
type DeprecatedContextReadHook = (field: string) => void;
/**
 * Build the `InitializationContext` from the INIT_SDK snapshot. The four launch
 * intent fields (`launchParams`, `shareParams`, `notificationParams`,
 * `shareLinkId`) are defined as deprecation-warning getters over the snapshot
 * values: on first read per field per session they `console.warn` once and
 * invoke `onDeprecatedRead`. They still return the real INIT value, so behavior
 * is unchanged. These getters are the legacy snapshot read path — they are NOT
 * backed by the launch-signal latch and may differ from `resolveLaunchIntent`
 * for a deferred link (that is the entire reason the new API exists).
 */
declare function buildInitializationContext(snapshot: InitializationContextSnapshot, onDeprecatedRead?: DeprecatedContextReadHook): InitializationContext;
interface InitResponse {
    initializeAsleep: boolean;
    instanceId: string;
    /** Game ID — used by multiplayer API to connect to the correct room server */
    gameId?: string;
    /** Room server URL for multiplayer WebSocket connections */
    roomServerUrl?: string;
    profile: {
        id: string;
        username: string;
        avatarUrl?: string | null;
        isAnonymous?: boolean;
    };
    safeArea?: SafeArea;
    launchParams: Record<string, string> & {
        /** Present when this game was launched via game-to-game navigation. The games (source and destination) define the contract. */
        g2gLaunch?: Record<string, any>;
        g2gReturn?: Record<string, any>;
    };
    shareParams: Record<string, string>;
    notificationParams?: Record<string, string>;
    shareLinkId?: string;
    environment: RawEnvironmentInfo;
    device: DeviceInfo;
    locale: string;
    languageCode: string;
    rooms?: {
        gameType?: string;
        rulesPreset?: string;
        matchmaking?: Record<string, any>;
        createOptions?: Record<string, any>;
        privateMatchDefaults?: Record<string, any>;
        [key: string]: any;
    };
}

interface CaptureConsentRequestOptions {
    includesCamera?: boolean;
}
/**
 * Host-specific port for the per-app, creator-tier-gated capture consent.
 * Implementations: RPC (RemoteHost), browser (PlaygroundHost), mock.
 */
interface CaptureConsentPort {
    /** Inspect current per-app consent without prompting. T1 -> granted. */
    get(): Promise<CaptureConsent>;
    /** Trigger the host's native harness (T2 and below) or browser, and return the new state. */
    request(opts?: CaptureConsentRequestOptions): Promise<CaptureConsent>;
    /** Persist consent server-side without showing the modal. Used for silent persist after the
     *  browser/OS permission prompt already served as the user's single opt-in. */
    set(status: 'granted' | 'denied'): Promise<CaptureConsent>;
}

interface ClipsDeps {
    files: FilesApi;
    ugc: UgcApi;
    captureConsent: CaptureConsentPort;
}
interface RecorderLike {
    isRecording(): boolean;
    useGameAudio(node: AudioNode): void;
    start(options?: StartClipRecordingOptions): Promise<void>;
    captureFrame(): void;
    stop(): Promise<ClipBlob>;
    cancel(): void;
}
declare class ClipsApiImpl implements ClipsApi {
    private readonly deps;
    private readonly recorder;
    constructor(deps: ClipsDeps, recorderFactory?: () => RecorderLike);
    isSupportedAsync(): Promise<ClipsSupport>;
    getCaptureConsentAsync(): Promise<CaptureConsent>;
    requestCaptureConsentAsync(opts?: {
        includesCamera?: boolean;
    }): Promise<CaptureConsent>;
    useGameAudio(node: AudioNode): void;
    captureFrame(): void;
    cancelRecordingAsync(): Promise<void>;
    startRecordingAsync(options?: StartClipRecordingOptions): Promise<void>;
    /**
     * Throws CAPTURE_CONSENT_DENIED if consent is denied or the platform dialog
     * is declined. Otherwise resolves and recording may proceed.
     *
     * Logic:
     * - T1 / already granted (get() returns granted) → proceed
     * - Already denied → throw
     * - Device permission is 'prompt' → skip platform dialog; the browser/OS shows
     *   its own prompt during getUserMedia (single opt-in). The host cannot
     *   securely self-grant from a game signal, so app consent stays
     *   'undetermined' and the platform dialog shows on the next recording.
     * - Device permission is 'denied' → skip, let getUserMedia fail naturally
     * - Device permission is 'granted' → show the platform dialog (no system
     *   dialog is coming, so this is the user's single opt-in)
     */
    private ensureCaptureConsent;
    stopRecordingAsync(persist?: ClipPersistOptions): Promise<ClipResult>;
    publishClipAsync(ugcId: string): Promise<UgcEntry>;
}

declare class ClipRecorder {
    private active;
    private gameAudioNode;
    isRecording(): boolean;
    useGameAudio(node: AudioNode): void;
    start(options?: StartClipRecordingOptions): Promise<void>;
    /** Adds one mixed audio track (game + mic) or a single source track to recordStream. */
    private attachAudio;
    /** captureMode 'manual': snapshot the game canvas into the recording surface. */
    captureFrame(): void;
    stop(): Promise<ClipBlob>;
    cancel(): void;
    /** Ends capture (stops recorder + releases mic/audio) but RETAINS chunks for stop(). */
    private cap;
    private startMaxDurationWatcher;
    /** Auto-mode composite loop: draws game canvas + PiP each frame. */
    private startCompositeLoop;
    private drawPip;
    private drawRoundRect;
    /**
     * Creates a fixed-position live camera preview visible at the top of the viewport.
     * Appended to document.body so no app element can hide or re-layer it.
     */
    private createPreviewOverlay;
    /** Idempotent: stops mic/canvas/webcam tracks, disconnects audio nodes, removes overlay. */
    private release;
}

declare class MockClipsApi implements ClipsApi {
    isSupportedAsync(): Promise<ClipsSupport>;
    getCaptureConsentAsync(): Promise<CaptureConsent>;
    requestCaptureConsentAsync(_opts?: {
        includesCamera?: boolean;
    }): Promise<CaptureConsent>;
    useGameAudio(_node: AudioNode): void;
    startRecordingAsync(_options?: StartClipRecordingOptions): Promise<void>;
    captureFrame(): void;
    stopRecordingAsync(_persist?: ClipPersistOptions): Promise<ClipResult>;
    cancelRecordingAsync(): Promise<void>;
    publishClipAsync(_ugcId: string): Promise<UgcEntry>;
}

declare class RpcCaptureConsent implements CaptureConsentPort {
    private readonly rpcClient;
    constructor(rpcClient: RpcClient);
    get(): Promise<CaptureConsent>;
    request(opts?: CaptureConsentRequestOptions): Promise<CaptureConsent>;
    set(status: 'granted' | 'denied'): Promise<CaptureConsent>;
}

/**
 * PlaygroundHost impl: the web playground has no native harness — the browser shows the
 * real getUserMedia prompt itself, so app-consent is treated as granted.
 */
declare class BrowserCaptureConsent implements CaptureConsentPort {
    get(): Promise<CaptureConsent>;
    request(_opts?: CaptureConsentRequestOptions): Promise<CaptureConsent>;
    set(status: 'granted' | 'denied'): Promise<CaptureConsent>;
}

declare class MockCaptureConsent implements CaptureConsentPort {
    private status;
    constructor(status?: CaptureConsentStatus);
    get(): Promise<CaptureConsent>;
    request(_opts?: CaptureConsentRequestOptions): Promise<CaptureConsent>;
    set(status: 'granted' | 'denied'): Promise<CaptureConsent>;
}

declare function initializeClips(rundotGameApi: RundotGameAPI, host: Host): void;

export { AccessGateApi, type AccessGateConfig, AccessTier, ActivityActionEvent, ActivityApi, ActivityIntent, AddToHomeScreenResult, AdminAudioGenApi, AdminGenBrowseParams, AdminGenBrowseResponse, AdminGenListReportsParams, AdminGenListReportsResponse, AdminGenResolveAction, AdminImageGenApi, AdminSpriteGenApi, AdminThreeDGenApi, AdminUgcApi, AdminUgcBrowseParams, AdminUgcBrowseResponse, AdminUgcListReportsParams, AdminUgcListReportsResponse, AdminUgcResolveAction, AdminVideoGenApi, AdsApi, AiApi, AiChatCompletionData, AiChatCompletionRequest, AiChatCompletionStreamChunk, AiChatCompletionStreamOptions, AnalyticsApi, AnimateSpriteParams, AnimateSpriteResult, AppApi, AppRole, AssetManifest, AssetUrlResult, Avatar3dApi, Avatar3dConfig, Avatar3dEdits, AwakeCallback, BackButtonCallback, BaseCdnApi, BrowserCaptureConsent, CanShareFileResult, CaptureConsent, type CaptureConsentPort, type CaptureConsentRequestOptions, CaptureConsentStatus, CdnApi, type CdnUrlShape, CharacterAnimateParams, CharacterWorkflow, type ChunkedAssetSource, ClipBlob, ClipPersistOptions, ClipRecorder, ClipResult, ClipsApi, ClipsApiImpl, ClipsSupport, CollectRecipeResult, CollectibleCard, CollectiblesApi, CommentsPanelResult, ComposeSocialPostOptions, ComposeSocialPostResult, CreateRoomOptions, CreditBalance, CreditPlansCatalog, CreditSubscription, CreditsApi, CreditsBillingContext, CreditsPurchaseResult, DEFAULT_SHARED_LIB_CDN_BASE, type DeprecatedContextReadHook, DepthEstimationParams, DepthEstimationResult, DeviceApi, DeviceInfo, DirectPurchaseSku, DualEmitStorageApi, EMBEDDED_LIBRARIES, EMBEDDED_LIBRARY_BY_KEY, type EmbeddedLibraryDefinition, EndActivityInput, Entitlement, EntitlementApi, EnvironmentApi, EnvironmentInfo, ExecuteRecipeOptions, ExecuteRecipeResponse, ExecuteScopedRecipeOptions, ExecuteScopedRecipeResult, Experiment, ExperimentAssignment, FILE_EXTENSION_PATTERN, FeaturesApi, FetchFromCdnOptions, FollowMePlatform, FollowMeResult, GamepadApi, GamepadConnectionEvent, GamepadLatencyStats, type GamepadReader, type GamepadRpc, GamepadSnapshot, type GatableHost, GenerationCostEstimate, GenerationCostEstimateRequest, GetActiveRunsOptions, GetAvailableRecipesOptions, GetAvailableRecipesResult, GetBatchRecipeRequirements, GetCurrencyIconOptions, GetFutureTimeOptions, GetPagedScoresOptions, GetPodiumScoresOptions, GrantInfo, HASH_ALGORITHM_NODE, HASH_ALGORITHM_WEB_CRYPTO, HapticFeedbackStyle, HapticsApi, Host, HostCdnApi, HostDeviceApi, HostEnvironmentApi, HostProfileApi, HostSystemApi, HostTimeApi, HttpCreditsApi, IapApi, IdentityChangedCallback, IdentityChangedEvent, ImageGenApi, ImageGenEntry, ImageGenJobEvent, ImageGenParams, ImageGenReport, ImageGenResult, type InitResponse, InitializationContext, type InitializationContextSnapshot, InitializationOptions, JoinOrCreateResult, JoinOrCreateRoomOptions, LatencyTracker, type LatencyTrackerOptions, LaunchIntent, LeaderboardApi, LedgerEntry, type LegacyGlobalMethodIds, LifecycleApi, LikeDialogResult, LikeStateResult, ListRoomsOptions, LiveOpsApi, LiveOpsConfigResult, LiveOpsRawConfig, LiveOpsSection, LoadEmbeddedAssetsResponse, LoggingApi, MIN_CDN_PATH_SEGMENTS, MODULE_TO_LIBRARY_SPECIFIERS, type MethodIds, MockAccessGateApi, MockActivityApi, type MockActivityApiOptions, MockAdminImageGenApi, MockAdminSpriteGenApi, MockAdminThreeDGenApi, MockAdminUgcApi, MockAdsApi, MockAiApi, MockAnalyticsApi, MockAppApi, MockCaptureConsent, MockCdnApi, MockClipsApi, MockCollectiblesApi, MockCreditsApi, MockDebug, MockDeviceApi, MockEntitlementApi, MockEnvironmentApi, MockFeaturesApi, MockGamepadApi, MockHapticsApi, MockIapApi, MockImageGenApi, MockLeaderboardApi, MockLifecycleApi, MockLiveOpsApi, MockLoggingApi, MockNavigationApi, MockNotificationsApi, MockPopupsApi, MockPreloaderApi, MockProfileApi, MockSharedAssetsApi, MockShopApi, MockSocialApi, MockSpriteGenApi, MockStatsApi, MockStorageApi, MockSystemApi, MockAiApi as MockTextGenApi, MockTimeApi, MockVideoApi, type ModuleSpecifierConfig, type ModuleSpecifierMatch, MultiplayerApi, NavigateToGameOptions, NavigationApi, NavigationStackInfo, type NotificationContent, NotificationsApi, OpenPaywallOptions, OpenStoreResult, PER_APP_LOCAL_HOST_PATTERN, PER_APP_SUBDOMAIN_HOST_PATTERN, PagedScoresResponse, type ParsedCdnLocation, PauseCallback, PlayerRankOptions, PlayerRankResult, PlaygroundProfileApi, PodiumScoresResponse, PopupsApi, PreloaderApi, ProfileApi, PromptCompletionRequest, PromptLoginResult, ProposeMoveRequest, ProposeMoveResult, ProposedMoveEvent, PurchaseSubscriptionResponse, PushAppOptions, QRCodeResult, QuitCallback, QuitOptions, RCSAvailabilityStatus, RawEnvironmentInfo, Recipe, ReleaseNote, RemoteHost, RemoveBackgroundParams, RemoveBackgroundResult, RequestRCSOptInInput, RequestRCSOptInResult, ResetStateOptions, ResetStateResult, ResolveLaunchIntentOptions, ResumeCallback, RoomDataUpdate, RoomMessageEvent, RoomMessageRequest, type RoomNotificationSubscriptionState, RoomSubscriptionOptions, RoomsApi, RpcAccessGateApi, RpcActivityApi, RpcAdminImageGenApi, RpcAdminSpriteGenApi, RpcAdminThreeDGenApi, RpcAdminUgcApi, RpcAdsApi, RpcAiApi, RpcAnalyticsApi, RpcAppApi, RpcAvatarApi, RpcCaptureConsent, RpcClient, RpcCollectiblesApi, RpcCreditsApi, RpcEntitlementApi, RpcFeaturesApi, RpcGamepadApi, type RpcGamepadApiOptions, RpcHapticsApi, RpcIapApi, RpcImageGenApi, RpcLeaderboardApi, RpcLifecycleApi, RpcLiveOpsApi, RpcLoggingApi, type RpcMessage, RpcNavigationApi, RpcNotification, RpcNotificationsApi, type RpcNotificationsApiOptions, RpcPopupsApi, RpcPreloaderApi, RpcRequest, RpcResponse, RpcRoomsApi, RpcShopApi, RpcSimulationApi, RpcSocialApi, RpcSpriteGenApi, RpcStatsApi, RpcStorageApi, RpcAiApi as RpcTextGenApi, RpcVideoApi, RunSubscriptionsResponse, RundotGameMessageId, RundotGameRoom, SDK_VERSION, SafeArea, SandboxProfileApi, ScheduleLocalNotification, ScheduleNotificationOptions, ScheduleRCSInput, ScheduleRCSResult, ScoreToken, ServerTimeData, ShareClickData, ShareFileOptions, ShareFileResult, ShareLinkResult, ShareMetadata, SharedAssetsApi, SharedStorageHostApi, ShopApi, ShopOrderHistoryResponse, ShopPurchaseResponse, ShopPurchaseResult, ShowEditorOptions, ShowInterstitialAdOptions, ShowRewardedAdOptions, ShowToastOptions, type ShowToastResponse, SimulationApi, SimulationAssignment, SimulationAvailableItem, SimulationBatchOperation, SimulationBatchOperationsResult, SimulationPowerPreview, SimulationRunSummary, SimulationSlotContainer, SimulationSlotMutationResult, SimulationSlotValidationResult, SimulationState, SleepCallback, SocialApi, SocialQRCodeOptions, SpendCurrencyOptions, SpendCurrencyResult, SpriteGenApi, SpriteGenCosts, SpriteGenEntry, SpriteGenJobEvent, SpriteGenParams, SpriteGenReport, SpriteGenResult, StartActivityResult, StartClipRecordingOptions, StartRoomGameOptions, StatsApi, StorageApi, type StorageType, StorefrontItem, StorefrontResponse, SubmitMessageInput, SubmitMessageResult, SubmitScoreParams, SubmitScoreResult, Subscription, SubscriptionInterval, SubscriptionTier, SystemApi, TextGenApi, AiChatCompletionData as TextGenChatCompletionData, AiChatCompletionRequest as TextGenChatCompletionRequest, PromptCompletionRequest as TextGenPromptCompletionRequest, ThreeDGenEntry, ThreeDGenReport, TimeApi, TriggerRecipeChainOptions, Unsubscribe, UpdateActivityInput, UpdateRoomDataOptions, UpscaleImageParams, UpscaleImageResult, ValidateMoveResult, ValidateMoveVerdict, VideoApi, VipClaimResult, WebGamepadReader, applyAccessGates, base64DecodeInto, base64ToArrayBuffer, base64ToUtf8, buildInitializationContext, computeScoreHash, createAccessGatedApi, createMockStorageApi, gateStreaming, getLibraryDefinition, initializeAccessGate, initializeActivity, initializeAds, initializeAi, initializeAnalytics, initializeApp, initializeAvatar3d, initializeCdn, initializeClips, initializeCollectibles, initializeCredits, initializeEntitlements, initializeFeaturesApi, initializeGamepad, initializeHaptics, initializeIap, initializeImageGen, initializeLeaderboard, initializeLifecycleApi, initializeLiveOps, initializeLocalNotifications, initializeLoggingApi, initializePopups, initializePreloader, initializeProfile, initializeRoomsApi, initializeShop, initializeSimulation, initializeSocial, initializeSpriteGen, initializeStackNavigation, initializeStats, initializeStorage, initializeSystem, initializeTextGen, initializeTime, initializeVideo, isPacificDaylightTime, isPerAppSubdomain, isUnsupportedMessageError, mapSubmitScoreFailure, mockLog, normalizeStandardGamepad, parseCdnLocationFromUrl, parseCdnLocationFromWindow, parseCdnPathSegments, readAssetInChunks, sanitizeProfile, setupRoomNotifications, wireIdentityReplica };
